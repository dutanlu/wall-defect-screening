# ⏸ 训练暂停状态（2026-09-23 20:18）

> 用户指示「先暂停」。**训练与自动化任务均已停止**，进度完整保留。

## 停在哪里

| 项 | 值 |
|---|---|
| 实验 | `v11s640_clsbal`（类平衡损失，`cls_pw=0.5`） |
| 已完成轮数 | **94 / 100**（`results.csv` 共 95 行，含表头） |
| 最后一轮 | ep94 |
| `last.pt` | `04_results/train/v11s640_clsbal/weights/last.pt`（76,023,010 B，**时间戳 20:17:09**） |
| `results.csv` | 完整保留 |
| 停止方式 | 先停接力器、后停训练进程（**均为 Force 终止**，非正常退出） |

## ⚠️ 一个必须知道的后果

训练是**被强制终止**的，不是正常跑完 100 轮，因此：

1. **`03_weights/v11s640_clsbal_best.pt` 未生成** ——
   `train.py` 只在训练**正常结束**时才把 `best.pt` 归档到 `03_weights/`（源码 L412-416）。
   ⇒ 续跑前若要评估，得直接用
   `04_results/train/v11s640_clsbal/weights/best.pt`。
2. `last.pt` 是**第 94 轮结束时**保存的，完好可用（ultralytics 每轮结束都会写）。

## 如何续跑（还剩 6 轮，约 12 分钟）

```powershell
cd "D:\pythonstudy 备份\创新题\外墙缺陷筛查"
& "D:\下载\python.exe" "02_code\train.py" --runs=v11s640_clsbal --resume
```

> ⚠️ `--resume` 后 `results.csv` 的 `time` 列会**从 0 重计**，
> 算每轮耗时要用**相邻两轮的绝对差值**，不能跨 resume 点做差。

## 自动化任务状态

「类平衡实验收口检查（`v11s640_clsbal`）」—— **已设为 PAUSED**。
（id `a9387ff8-7b12-4c8d-bd41-54de41a715f3`，原定 21:00 执行）
恢复方式：让我把它改回 ACTIVE，或直接手动执行收口。

## 收口清单（训练跑满 100 轮后要做的）

1. `02_code/evaluate.py` 在独立测试集上评测 → `v11s640_clsbal_test_metrics.json`
   （**用绝对路径 + 参数加引号**，因为它按工作目录解析 `--weights`）
2. `logs/_cmp_clsbal_ab.py` → A/B 逐类对比
3. `logs/_fill_sec78.py` → 回填 `07_report/技术报告.md` §7.8 结果表
   （先 `--dry-run` 看一眼）
4. `logs/_patch_ppt_clsbal.py` + 重新生成 PPT（技术深度页加「⑤ 损失函数优化」）

## 判读纪律（收口时不得违反）

- 整体 mAP50 差 **< 0.03** 一律写「差异在噪声内」，**不得**写「提升了 mAP」。
- ✅ 可主张：「针对 45.6:1 的类别长尾，实现并验证了逆频率类平衡损失（`cls_pw=0.5`），
  把有效损失占比从 45.6:1 收敛到 6.75:1」—— 可复算的工程事实。
- ❌ 不得主张「提升了 mAP」。本项拿的是「技术深度·损失函数优化」的分。
- 口径：只能写「**有效变量为 `cls_pw`**」（因前 8 轮 `workers=4`、第 9 轮起 `workers=0`），
  **不得**写「唯一变量 = cls_pw」。

## 暂停时的进度参考（val 口径，仅供了解趋势）

最后 10 轮均值：实验 mAP50 0.6779 / Recall 0.6365；
基线 mAP50 0.6812 / Recall 0.6324。
⇒ 差 mAP50 **−0.0033**、Recall **+0.0041** —— **收敛到几乎同一水平**，符合预期
（类平衡调整的是训练中的梯度分配，充分训练后收敛水平相近；recall 略高方向正确但幅度不足）。
**这些是 val 过程曲线，最终结论必须等 test 口径。**
