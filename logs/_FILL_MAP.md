# V2 指标回填映射表（⏳ → 数值）

> 状态：**等待中**。指标一到，按本表逐格填，不凭印象写任何文件名或数字。
> 数据源唯一权威：`logs/_v2_metrics_snapshot.json`（由 `_collect_v2_metrics.py` 生成）。
> 生成时刻：2026-09-20 18:45 版（届时请以最新快照为准）。

---

## 0. 硬纪律（每条都踩过）

1. **只能引用 `split=test` 的指标**（67 图 / 259 实例）。`val`（134 图 / 501 实例）
   **仅用于判断收敛**，永不对外上报。
2. **差值 < ≈0.03 一律不解读为「更好」**（见报告 §7.1.1 的置信区间半宽）：
   efflorescence ±0.08 / crack ±0.11 / moss ±0.12 / **spalling ±0.16**。
3. **每类 AP 一律按类名对齐**，不按 id 对号入座（本项目踩过 id 顺序错配）。
4. **0 样本类别（exposed_rebar / rust / delamination）写「—」**，不写 0.0000。
5. **引用的文件名必须真实存在** —— 回填前先确认路径（本项目踩过
   `pipeline_bench_up6.json` 根本不存在却被写进报告）。

---

## 1. `技术报告.md` 逐处映射

| # | 位置 | 当前状态 | 填入什么 | 数据源 |
|---|---|---|---|---|
| 1 | §5.3 三档对照表（约 `:922-926`） | ⏳ / V1 口径 | 四模型 体积 + mAP50 + mAP50-95 | `eval/<key>_test_metrics.json` → `overall`、`weights_mb` |
| 2 | §7.1 每类 AP 表 | 过渡口径（V1 权重） | 换 V2 权重的 `per_class` AP50 / AP50-95 | `eval/v8s640_test_metrics.json` → `per_class` |
| 3 | §7.1 三档对照表（约 `:922`，与 #1 同表） | 已清空 | 见 #1 | — |
| 4 | §7.2 域偏移表 | V1 | 4.0mm/px 崩溃点等 | `ablation/` 相关 JSON |
| 5 | §7.3 鲁棒性四组三档（12 配置） | V1 | 12 格 | `ablation/` 相关 JSON |
| 6 | §7.4 三组消融（含两个负结果） | V1 | A/B/C 三组 | `ablation/ablation_{A,B,C}_*_v8s640.json` |
| 7 | §8.2 量化达标结论 | V1 | 两者是否达标 + 体积/延迟 | `04_results/quant/*.json` |
| 8 | 附录 A「已完成/未完成」两表 | 部分 ⏳ | 把 #1~#7 全部转 ✅ | — |

## 2. `答辩材料.md` 逐处映射

| # | 位置 | 填入什么 |
|---|---|---|
| 1 | `:87-93` 三档对照表 | 同技术报告 #1 |
| 2 | `:112` `v8s@1024` 补充实验 | `v8s1024` 的 test mAP50 + 结论（提高分辨率是否改善小目标） |
| 3 | `:119` 量化两条路 | 同技术报告 #7 |
| 4 | `:5` 顶部状态行 | 把「⏳ 仅剩 v8s@1024 在跑」改为「✅ 全部回填完成」 |

## 3. `README.md` 逐处映射

| # | 位置 | 填入什么 |
|---|---|---|
| 1 | `:41` v8s test mAP50 | `v8s640` test `overall.map50` |
| 2 | `:42` v8s mAP50-95 | 同上 `map50_95` |
| 3 | `:44` v11s test mAP50 | `v11s640` test `overall.map50`（**删掉「互有胜负」类表述**） |
| 4 | `:45` v8n test mAP50 | `v8n640` test `overall.map50`（若仍异常则保留「无有效值」并说明） |
| 5 | `:47` 鲁棒性最低档 | 同技术报告 #5 |
| 6 | `:29-31` 顶部 V2 状态块 | 改为「✅ V2 已完成」 |

## 4. 回填后必做的三项复核

1. **残留检查**：全报告搜 `⏳`、`V1 口径`、`待回填`、`待测`，逐一确认清零或已注明理由。
2. **交叉验证**：每类 `instances` 合计必须 = **259**；图片合计必须 = **67**
   （与 `step4_split` 两条独立路径一致性）。
3. **口径声明**：新增的每个 AP 数字附近，确认 §7.1.1 的不确定性声明仍在有效范围内
   （尤其 spalling 的 ±0.16）。

## 5. 预计会被问到的问题（提前想好）

- **「v8s 和 v11s 谁更准？」** ⇒ 若差值 <0.03，如实答「在本测试集规模下判别不出来」，
  改从体积（21.51 vs 18.33 MB）、生态成熟度、可用性论证。
- **「spalling 的 AP 怎么比别的类低？」** ⇒ 测试集只有 **30 实例 / 19 图**，
  置信区间半宽达 ±0.16 —— **是样本量问题，不是模型问题**。
