
### 2026-09-26 09:35–09:45（同一轮巡检的收尾：MEMORY.md 超限处置）
- `MEMORY.md` 注入时被截断（只到 §2.4 前）⇒ 做**第十三次精简**：
  **§一（环境与工具纪律）+ §二（ultralytics 踩坑）全文逐字搬入 `_MEMORY_retired_20260926c.md`**
  （10919 B / 纯 LF），正文只留一行提要索引；备份 `MEMORY_backup_20260926_0927.md`（21168 B）。
  **纪律零删除**；正文 21168 B / 12591 字符 → **14733 B / 8586 字符** ⇒ 回到注入预算（≈10000 字符）内。
  脚本 `logs/_mem_compact13.py`（字节级操作 + 前缀/尾部/行尾全断言）。
- ★★ **新纪律（已写入 MEMORY 速查 #5）：量行尾不要用 `grep -c $'\r$'`**。
  本次实测它把**纯 LF** 的 `MEMORY.md`（0 CR / 268 LF）报成「CRLF=268」，据此写出的脚本
  差点把 memory 目录的文件从纯 LF 改成混合行尾 ⇒ **只认 `read_bytes()` 的 `count(b"\r")`**。
  （断言 `crlf==0` 当场拦下，已从备份还原后按 LF 重做。）
- ★ **memory 目录惯例确认**：`MEMORY.md`、`_MEMORY_retired_*.md` 均为**纯 LF**；
  工作区 `2026-09-25.md` / `2026-09-26.md` 为**纯 CRLF**。两者不同，勿统一。
