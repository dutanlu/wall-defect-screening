# `_EXPERIMENTS_SUMMARY.md` 生成说明与一次体积异常的坦白记录

## 这个文件怎么来的

- **唯一生成器**：`logs/_report_experiments.py`（只读 `04_results/eval/*.json`，不做任何口径改动）。
  重新生成命令：`"D:/下载/python.exe" -X utf8 logs/_report_experiments.py`
- ⇒ **不要手工编辑 `_EXPERIMENTS_SUMMARY.md`**：下次重跑会整体覆盖，手写内容会静默丢失。

## 2026-09-26 04:13 重新生成时发生的事（如实记录）

| 项 | 值 |
|---|---|
| 重新生成前（mtime 2026-09-25 22:38:48） | **10263 B** |
| 重新生成后（mtime 2026-09-26 04:13:57） | **4566 B** |
| 生成器（当前版本，已含 item#1） | 只能产出总览 / 分组对比 / 露筋专项 / 读数须知 |

**体积缩水 5697 B 的原因未查明，且旧版内容未能找回**：
- 旧版**没有**被 git 跟踪（`logs/` 是跟踪目录，但该文件从无提交记录）⇒ `git show` 取不到；
- 全盘（`D:\pythonstudy 备份`）只有一份 `_EXPERIMENTS_SUMMARY.md`，交付包里也没有副本；
- 用当前生成器**无法复现 10263 B**（当前 RUNS 为 4 项时约 4 KB，10 项时约 4.5 KB）
  ⇒ 推断旧版**含生成器之外的内容**（很可能是某次会话手工追加的说明/声明段，
  例如 imgsz confound 的展开解释），该部分**已随本次覆盖丢失**。

**未做过度补救的理由**：证据（`_EXPERIMENTS_SUMMARY.json`，09-25 13:24）显示旧版的实验集合
就是那 4 个 run，全部仍在新版总览表内；新版还多出 item#1 的两个 100 轮口径与 4 个待跑种子行
⇒ 新版在**实验覆盖面**上不缺，缺的只可能是散文式说明。

## 下次怎么办

1. 若要保留生成器写不出的手写说明 ⇒ **另存独立文件**（如本 NOTE），不要写进 `_EXPERIMENTS_SUMMARY.md`。
2. 生成器改动后，重新生成前先 `cp` 一份带时间戳的备份，便于比对体积异常。
3. 体积突变（如本次 −55%）应视为**需要人查的信号**，不要默默接受。
