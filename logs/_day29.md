
## 二十九、⏸ 训练暂停（用户指示，2026-09-23 20:18）

### 用户指示
「能不能先暂停」→ 确认后选择「**两样都停**」（训练 + 自动化）。

### 执行
1. **先停接力器**（必须先停：否则它检测到训练结束会自动拉起下一块）；
2. **再停训练进程**；两者均 Force 终止。
3. 复查：clsbal 相关进程 **0 个**。
4. **automation 设为 PAUSED**（id `a9387ff8-…`，原定 21:00）。

### 停在哪
| 项 | 值 |
|---|---|
| 完成轮数 | **94 / 100** |
| `last.pt` | 完好，76,023,010 B，时间戳 **20:17:09**（第 94 轮结束时保存） |
| `results.csv` | 完整保留 |

### ★ 必须记住的后果：`best.pt` 未归档
训练是**被强制终止**的（非正常跑完），而 `train.py` **只在训练正常结束时**
才把 `best.pt` 复制到 `03_weights/`（源码 L412-416）。
⇒ 因此 **`03_weights/v11s640_clsbal_best.pt` 不存在**。
⇒ 若要现在就评估，直接用 `04_results/train/v11s640_clsbal/weights/best.pt`。
（这条已写进 `logs/_PAUSED_类平衡训练.md`，收口时不会再踩。）

### 续跑方式（还剩 6 轮，约 12 分钟）
```
cd "D:\pythonstudy 备份\创新题\外墙缺陷筛查"
& "D:\下载\python.exe" "02_code\train.py" --runs=v11s640_clsbal --resume
```

### 内存释放情况（与预期有差距，值得记录）
停之前 2.45 GB 可用 → 停之后 **2.88 GB**，仅释放约 0.4 GB。
**原因**：训练进程的「私有内存 7.3 GB」里**大部分驻留在页面文件**，
工作集只有 731 MB ⇒ 物理内存本来就没被它占着。
⇒ **教训：看「工作集」才是看物理内存占用；「私有内存」包含已换出的部分，
   不能直接当作「停掉能释放多少 RAM」的依据。**

### 暂停时的进度参考（val 口径）
最后 10 轮均值：实验 mAP50 **0.6779** / R **0.6365**；
基线 mAP50 0.6812 / R 0.6324。差：mAP50 **−0.0033**、R **+0.0041**。
⇒ **收敛到几乎同一水平**，与 §7.8 里预先写下的「很可能测不出精度差异」的预期吻合；
recall 略高方向正确但幅度不足。**最终结论必须等 test 口径。**

### 收口清单已落盘
详见 `logs/_PAUSED_类平衡训练.md`（含评估/AB/回填/PPT 四步 + 判读纪律 + 口径说明）。
