# 标记感知 lint（只读）

判定规则：旧口径数字若在 ±2 行窗口内出现 V2/历史/口径/旧/⏳/不可比 等标记，
视为**刻意保留**；否则列为**待处理遗漏**。

## 07_report\技术报告.md（2495 行 / 编码 utf-8-sig）

- **真正未标注：0 处** ⬅ 只看这个数
- 行邻已标注：34 处
- 仅节级横幅覆盖（正常）：7 处
- ⏳ 回填点：1 处

### 🟡 靠节级横幅覆盖（刻意保留，仅列行号）

- `L278` 【2494（V2 总框数）】→ 属节 `#### 4.1.1 数据清洗各阶段的实测数量（V3 口径：7 类全部有数据）`
- `L280` 【674（V2 可用样本图数）】→ 属节 `#### 4.1.1 数据清洗各阶段的实测数量（V3 口径：7 类全部有数据）`
- `L281` 【473（V2 train 图数）】→ 属节 `#### 4.1.1 数据清洗各阶段的实测数量（V3 口径：7 类全部有数据）`
- `L295` 【674（V2 可用样本图数）】→ 属节 `#### 4.1.1 数据清洗各阶段的实测数量（V3 口径：7 类全部有数据）`
- `L1206` 【259（V2 test 实例数）】→ 属节 `#### 7.1.1 指标不确定性声明（**已按 V3 测试集重算**；与权重无关）`
- `L1443` 【67 图（V2 test 图数）】→ 属节 `### 7.4 消融实验（`exp_ablation.py`）`
- `L1590` 【67 图（V2 test 图数）】→ 属节 `### 7.5 端到端管线耗时实测（`bench_pipeline.py`）`

### ✅ 行邻已标注（刻意保留，仅列行号）

- `L269` 【「4 类」（V2 只有 4 类有数据）】→ **V2 历史链条（4 类时期，保留备查）**
- `L271` 【「只有 4 类」】→ 下表为 V3 之前、只有 4 个类别有数据时的清洗链（**V2 口径**），本报告旧版引用的是它：
- `L296` 【2494（V2 总框数）】→ > 实例数同样对得上：2415 + 90 − 11 = **2494**。
- `L320` 【「4 类」（V2 只有 4 类有数据）】→ 2. **原有 4 类的总量逐位不变**（469 / 313 / 1152 / 560），但**三集之间的分布有位移**
- `L446` 【674（V2 可用样本图数）】→ （整图丢弃后实际可划分 674 张 —— **此为 V2 口径**：本节是「定位并处置泄漏」
- `L459` 【259（V2 test 实例数）】→ > ⚠️ **跨版本不可比（重要）**：V2 测试集（67 图 / 259 实例）**只有 4 个类有实例**；
- `L465` 【67 图（V2 test 图数）】→ > 拿归档的 V2 测试集逐张算 MD5 后比对：V2 的 67 张测试图中，
- `L478` 【「4 类」（V2 只有 4 类有数据）】→ 统一体系设计的 7 类，**V3 起全部有数据支撑**（V2 时仅 4 类）：
- `L633` 【134（V2 val 图数）】→ > V3 val 为 **568 张 / 3985 实例**，单轮 mAP 由这 568 张决定（V2 为 134 张）。
- `L646` 【259（V2 test 实例数）】→ > V2 测试集为 67 图 / 259 实例 / 4 类有标注 —— 两版**不可直接比大小**（见 §4.3）。
- `L664` 【473（V2 train 图数）】→ | 数据 | 同一划分（**V2 为 473/134/67 图**；**V3 为 1995/568/285 图**）；跨集交集 9 项全为 0 |
- `L725` 【「4 类」（V2 只有 4 类有数据）】→ <!-- v3bf:t532 -->（**V2 口径**：回答「会不会把非本类判成这 4 类」）
- `L727` 【67 图（V2 test 图数）】→ 在 **V2 的 67 张测试图**（4 类有标注）上以 `conf=0.25`、`IoU≥0.5` 做贪心匹配，实测：
- `L731` 【259（V2 test 实例数）】→ | `v11s640` | 259 | 307 | 196 | **2** | **109** | 61 | **0.6384** |
- `L732` 【259（V2 test 实例数）】→ | `v8s640` | 259 | 312 | 192 | 1 | 119 | 66 | 0.6154 |
- `L739` 【「4 类」（V2 只有 4 类有数据）】→ > **无实例** —— 但这**不是因为**「模型只输出 4 类」；
- `L752` 【259（V2 test 实例数）】→ > ⚠️ **口径说明**：上表的假阳/跨类误判计数取自 **V2 测试集（67 图 / 259 实例 / 4 类）** 的
- `L779` 【259（V2 test 实例数）】→ > 是 **V2 测试集（67 图 / 259 实例 / 4 类）** 的口径。V3 扩到 285 图 / 7 类后，
- `L793` 【67 图（V2 test 图数）】→ （V2 的 67 图测试集上曾测得 +0.086 的差距，V3 扩容后该差距消失 —— 见 §5.3.1 的受控条件说明。）
- `L1053` 【259（V2 test 实例数）】→ > ⚠️ **但它们依赖数据集版本**：上表为 **V2 口径**（259 实例）。
- `L1057` 【259（V2 test 实例数）】→ > V2 的实例数合计 **259** 与 §4.1.1 的划分报告（test 259 实例）**完全一致** ——
- `L1148` 【259（V2 test 实例数）】→ 本项目的测试集为 **285 张 / 1646 个实例**（**V3 口径**；V2 为 67 张 / 259 实例），
- `L1173` 【259（V2 test 实例数）】→ **V2 测试集（67 图 / 259 实例 / 4 类）—— 历史对照**（§5.3 的论证仍引用这批数字）：
- `L1181` 【259（V2 test 实例数）】→ | *（整体，仅供参考）* | *259* | *≈ ±0.05* |
- `L1201` 【「4 类」（V2 只有 4 类有数据）】→ > **V2（4 类，历史对照）**：
- `L1228` 【259（V2 test 实例数）】→ > （259 实例）经验推出的**，不是严格的统计推断。V3 整体扩到 **1646 实例**后：
- `L1243` 【134（V2 val 图数）】→ `val` 只有 **134 张（V2 口径；V3 为 568 张）**，其实测 mAP50 在 ep 71-100 的波动区间为
- `L1254` 【259（V2 test 实例数）】→ 1646 个实例 / 7 类**（V2 为 67 张 / 259 实例 / 4 类）上得到的结论，
- `L1806` 【134（V2 val 图数）】→ `MemoryError: bad allocation`（attempts 134/128/96/64）。为区分
- `L2150` 【「4 类」（V2 只有 4 类有数据）】→ 报告只能表述为「设计了 7 类统一体系，其中 4 类有数据支撑」。
- `L2379` 【259（V2 test 实例数）】→ > 67 图 / 259 实例（4 类）换成 **285 图 / 1646 实例（7 类）**。
- `L2390` 【674（V2 可用样本图数）】→ | §4.3 | 数据泄漏处理与最终划分（674 对 / 473-134-67，**V2 口径**） | ✅ **V2 已更新** → ✅ **V3 数据事实已定稿**（§4.1.1 / §4.4） |
- `L2431` 【259（V2 test 实例数）】→ | ~~自检 P2-9~~ | ~~`evaluate.py` 每类 `instances` 恒为 `null`~~ | ✅ **已修复**：根因是 ultralytics `Metric` 类**没有 `np_per_class`**（旧代码 `getattr` 恒得 `None` 且**不报错**）；改用 `nt_per_class`
- `L2468` 【67 图（V2 test 图数）】→ ⚠️ **口径更正**：V2 测试集（67 图 / 4 类）上曾测得 `v11s` 高 0.086，

### ⏳ 回填点

- `L2439` → | ~~§7.5~~ | ~~端到端管线耗时实测~~ | ✅ **已完成**：新增 **§7.5**；`bench_pipeline.py` 分层计时，两档分辨率实测，一致性自检 0.85 / 1.01；答辩材料同步（原「⏳ 待补」已替换） |

## 07_report\答辩材料.md（440 行 / 编码 utf-8-sig）

- **真正未标注：0 处** ⬅ 只看这个数
- 行邻已标注：12 处
- 仅节级横幅覆盖（正常）：0 处
- ⏳ 回填点：0 处

### ✅ 行邻已标注（刻意保留，仅列行号）

- `L80` 【259（V2 test 实例数）】→ **答**：不是「选得最好的」，而是**同条件训练、同一独立测试集（**V2：67 图 / 259 实例**）
- `L83` 【259（V2 test 实例数）】→ ⚠️ **先说一条纪律（这条会让回答更可信）**：我们的**V2 测试集**（67 图 / 259 实例）里
- `L97` 【259（V2 test 实例数）】→ > ⚠️ **口径更正（2026-09-21）**：上表此前引用的是 **V2 测试集（67 图 / 259 实例 / 4 类）**
- `L210` 【259（V2 test 实例数）】→ **A**：先解释口径 —— 我们报的是**独立测试集**（**V2：67 图 / 259 实例**）指标，
- `L226` 【674（V2 可用样本图数）】→ 重新做 7:2:1 分层划分（整图丢弃后实际可划分 **674** 张，划成 **473 / 134 / 67**
- `L266` 【259（V2 test 实例数）】→ > 这条阈值**源自 V2 的样本量**（259 实例）。V3 整体扩到 1646 实例后，
- `L278` 【「4 类」（V2 只有 4 类有数据）】→ ### Q：为什么类别体系设计了 7 类，而早先只有 4 类有数据？（V3 起已补齐）
- `L283` 【674（V2 可用样本图数）】→ | V1 / V2 | **4 类**（crack / spalling / efflorescence / moss） | 674 图 / 2494 框 |
- `L311` 【259（V2 test 实例数）】→ >    与 V2 的 67 图 / 259 实例 / 4 类**不具可比性**。V3 数字已于 2026-09-21 回填；
- `L312` 【259（V2 test 实例数）】→ >    本材料如同时出现两版数字，凡「259 实例」「67 图」均属 V2 历史口径。
- `L398` 【67 图（V2 test 图数）】→ 1. 数据集（**V2 测试集口径**）**65/67 张都是 448×448**（Roboflow 预处理后的产物），
- `L437` 【「4 类」（V2 只有 4 类有数据）】→ 5. **不要承诺没做的**。V2 时 7 类里只有 4 类有数据就说 4 类；**V3 起 7 类全部有数据**，

## 07_report\README.md（217 行 / 编码 utf-8-sig）

- **真正未标注：0 处** ⬅ 只看这个数
- 行邻已标注：4 处
- 仅节级横幅覆盖（正常）：0 处
- ⏳ 回填点：0 处

### ✅ 行邻已标注（刻意保留，仅列行号）

- `L39` 【473（V2 train 图数）】→ | 数据集规模 | **1995 / 568 / 285** ✅ | train / val / test（**V3**；V2 为 473 / 134 / 67） |
- `L41` 【259（V2 test 实例数）】→ | **主力模型 `v11s640` 测试集 mAP50** | **0.7230** | V3 独立测试集 285 图 / 1646 实例（7 类有标注）；V2 为 67 图 / 259 实例（4 类），两版不可比 |
- `L170` 【「4 类」（V2 只有 4 类有数据）】→ V2 时只有 4 类（crack / spalling / efflorescence / moss）拿得到数据：
- `L174` 【674（V2 可用样本图数）】→ 数据集由 **674 图**扩到 **2848 图 / 7 类 / 18262 框**（见 §4.4 / §4.1.1）。

## 07_report\演示视频脚本.md（293 行 / 编码 utf-8-sig）

- **真正未标注：0 处** ⬅ 只看这个数
- 行邻已标注：4 处
- 仅节级横幅覆盖（正常）：0 处
- ⏳ 回填点：0 处

### ✅ 行邻已标注（刻意保留，仅列行号）

- `L194` 【「4 类」（V2 只有 4 类有数据）】→ > 第二，**我们设计的 7 类，早先只有 4 类拿得到数据**。
- `L198` 【674（V2 可用样本图数）】→ > 露筋、锈迹、空鼓三类的数据，数据集从 674 张扩到 **2848 张**，
- `L287` 【473（V2 train 图数）】→ 重切后 **V2：473 / 134 / 67** → **V3：1995 / 568 / 285**
- `L288` 【674（V2 可用样本图数）】→ （并入 HRCDS + Urban 后由 **674 图**扩到 **2848 图 / 7 类 / 18262 框**），

## 05_quantify_grade\README.md（138 行 / 编码 utf-8-sig）

- **真正未标注：0 处** ⬅ 只看这个数
- 行邻已标注：0 处
- 仅节级横幅覆盖（正常）：0 处
- ⏳ 回填点：0 处

## 06_deploy\README.md（58 行 / 编码 utf-8-sig）

- **真正未标注：0 处** ⬅ 只看这个数
- 行邻已标注：0 处
- 仅节级横幅覆盖（正常）：0 处
- ⏳ 回填点：0 处

---

## 汇总

| 文件 | 行数 | 未标注 | 已标注 | ⏳ |
|---|---:|---:|---:|---:|
| 07_report\技术报告.md | 2495 | 0 | 41 | 1 |
| 07_report\答辩材料.md | 440 | 0 | 12 | 0 |
| 07_report\README.md | 217 | 0 | 4 | 0 |
| 07_report\演示视频脚本.md | 293 | 0 | 4 | 0 |
| 05_quantify_grade\README.md | 138 | 0 | 0 | 0 |
| 06_deploy\README.md | 58 | 0 | 0 | 0 |

**未标注合计 0 处；⏳ 回填点合计 1 处。**

## ⏳ 回填点总表（按文件）

**07_report\技术报告.md**

- `L2439` | ~~§7.5~~ | ~~端到端管线耗时实测~~ | ✅ **已完成**：新增 **§7.5**；`bench_pipeline.py` 分层计时，两档分辨率实测，一致性自检 0.85 / 1.01；答辩材料同步（原「⏳ 待补」已替换） |
