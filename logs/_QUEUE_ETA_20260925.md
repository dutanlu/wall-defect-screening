# 队列工期实测（2026-09-25 23:10）

## 实测速率
从 `04_results/train/v11s640_b16/results.csv` 的 `time` 列反算（该列在 `--resume` 时**归零重计**）：
本次会话起点 = epoch 70（time=77.2 s），末行 epoch 79（time=643.8 s）
⇒ **(643.8 − 77.2) / 9 = 63.0 s/epoch**，且逐轮稳定（相邻差 ~62.8 s）。

| 配置 | 实测每轮 | 依据 |
|---|---|---|
| 640 / batch16 / workers4 | **63 s** | 本表 |
| 896 | **161 s** | 早前实测 `v11s896_r20` = 3219 s / 20 |

## 剩余工期（基准时刻 23:10）

| 阶段 | 计算 | 预计完成 |
|---|---|---|
| Run A `v11s640_b16` | 剩 21 轮 × 63 s ≈ 22 min | 约 **23:32** |
| Run B `v11s896_r100` | 100 轮 × 161 s ≈ 4.5 h | 约 **04:05** |
| A/B 评测 | 各约数分钟 | 约 04:15 |
| item#2 `seed_merge100` | 4 组 × 100 轮 × 63 s ≈ 7.0 h | 约 **11:15** |
| item#3 `tta_seeds` | 3 种子 × 75 s ≈ 4 min（零额外训练） | 约 11:20 |

## ★ 结论：**7:00 跑不完**，整条队列预计 **次日约 11:15** 收口

原因两条，都不是"卡住了"，而是**我先前把估算写乐观了**：
1. `logs/_watch_queue_0925.py` 的 docstring 写「item#1 ≈ 2 h」，
   但没算上 ① 22:44 崩溃杀掉训练、丢掉约 1 轮；② **Run B 是 896，成本是 640 的 2.3 倍**（4.5 h 而非 1.7 h）。
2. item#2 是 **4 次 100 轮训练**（s123/s2024 × 原数据/合并数据），不是 2 次。

> 处置：不缩小实验范围（用户明确要求 item#1 与 item#2 都做），
> 如实记录工期，并把「崩溃自愈」的判据从「两小时无变化」**收紧到 15 分钟**
> （63 s/轮 ⇒ 15 min ≈ 14 轮，足够区分"在跑"与"已死"；原判据会白等两小时）。

## 附带发现（值得记住）
**`results.csv` 的 `time` 列在 `--resume` 时会归零重计。**
⇒ 用它算均速时必须先定位"归零那一行"，否则会算出荒谬的 7.2 s/epoch（本次第一版就踩了）。
