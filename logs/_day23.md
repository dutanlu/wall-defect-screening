
## 二十三、训练恢复推进 + §7.8 进一步完善 + 收口脚本预测试（2026-09-23 17:4x）

### 1) 训练已恢复正常推进
- 17:37 用 **workers=0** 重启后，**17:41 撑过 epoch 9**，此后稳定推进：
  17:45 已完成 **11/100 轮**，内存 **1.96 GB 可用 / 87%**（用户关掉多余 WorkBuddy 窗口后
  从 1.41 GB 改善）。
- 进程结构：接力器 PID 50652 + 训练 PID 6968，**无 dataloader 子进程**
  ⇒ 再次证实 `workers=0` 生效。
- 每轮约 118–120 秒 ⇒ 剩余 89 轮 ≈ **3.0 小时**。

### 2) ★ §7.8.1 有一处表述错误，已修正
- 原文写「`spalling`（测试集里 CI 半宽最大、**最难检的那一类**）」—— **「最难检」是错的**。
- 按 V3 实测：`spalling` AP50 = **0.7734**（中上）；它只是**实例数最少（43）⇒ CI 半宽最大（±0.129）**。
  **真正最弱的是 `exposed_rebar`（AP50 0.4568，147 实例 / ±0.070）。**
- 已改为「口径更正」段落，并把实验目标表述为
  「**抬升长尾类的梯度占比**」而非「提升某类精度」。

### 3) ★ 三处「workers=4」过期文案已全部重写
把 `workers` 从 4 改 0 后，原来那句「实验 `workers=4`」不再成立。三处同步改为**新的事实**：
- `logs/_fill_sec78.py`（回填脚本的诚实说明段）
- `logs/_cmp_clsbal_ab.py`（A/B 报告头）
- `07_report/技术报告.md` §7.8.4（A/B 设置）

新文案要点（**必须与报告一致**）：
> ① 本实验**前 8 轮**用 `workers=4`，第 9 轮因 Dataloader worker 触发
> `cv::OutOfMemoryError` 中断；**第 9 轮起改为 `workers=0` 续训**（与基线口径一致）。
> `workers` 不改变**数据顺序、增强分布与损失数学**
> （`shuffle` 由主进程 generator 决定，训练用 `seed=42` + `deterministic=True`），
> 但**不是全程唯一变量** ⇒ 只能写「**有效变量为 `cls_pw`**」，不得写「唯一变量 = cls_pw」。
> ② `--resume` 使 `results.csv` 的 `time` 列从 0 重计 ⇒ 每轮耗时须用相邻两轮绝对差值算。

### 4) §7.8.3 补入 L3 逐类损失占比表（证据链加强）
从 `logs/_CLSBAL_VERIFY.txt` 取实测值：

| 类别 | 无加权 | 加权后 | 变化 |
|---|---|---|---|
| spalling | 16.9% | **26.3%** | **+9.4 pp**（提升最大） |
| crack | 14.4% | 18.7% | +4.3 pp |
| moss | 14.4% | 16.3% | +1.9 pp |
| delamination | 14.4% | 14.7% | +0.2 pp |
| efflorescence | 14.4% | 11.2% | −3.3 pp |
| exposed_rebar | 14.4% | 10.4% | −4.1 pp |
| rust | 10.9% | **2.5%** | **−8.4 pp**（压低最多） |

加权前后 cls 损失总和 614.34 → 640.00（比值 1.0418 ≠ 1）。
★ **同时加了「两表不许混读」的口径说明**：本表是**受控构造**（7 类样本数拉平，纯化权重效应），
**不等于训练时自然占比**（后者见 7.8.2：rust 17.9% / spalling 2.6%）。
两表回答不同问题：7.8.2 = 「真实分布下梯度预算如何重分配」；本表 = 「权重是否作用到每一类」。

### 5) ★ 收口脚本做了预防性测试（避免训练跑完才发现 bug）
- 给 `_fill_sec78.py` 加 `--exp=<path>` 参数（便于用假数据自测）。
- 构造假 json（模拟「整体几乎不变 + 稀有类略升 + exposed_rebar 略升」）跑 `--dry-run`，
  **输出全部正确**：整体表 / 逐类 CI 表 / 稀有类召回表 / 判读段 / 诚实说明段都正常，
  且正确给出「**逐类 7 类全部落在各自 CI 半宽内**」与「整体差异在噪声内」。
- 测试用假 json 已清理（`logs/_FAKE_clsbal_test_metrics.json`）。

### 6) 复算校验（确保报告数字可复算）
用 `counts=[299,207,841,973,9433,486,392]` 独立复算权重表：
`1.3492 / 1.6216 / 0.8045 / 0.7479 / 0.2402 / 1.0583 / 1.1783` —— 与报告**逐位一致**；
45.6:1 → 6.75:1 亦一致。**报告中的这些数字可直接复现。**
