# 赛题符合性复核报告（2026-09-24）

> **复核对象**：第六届萌新种子杯 · 视觉赛道 · 创新题（外墙缺陷筛查）+ 基础题（水果分拣）
> **复核依据**：
> - 创新题：`.workbuddy/reference/YOLO视觉应用创新赛_赛题_全文.txt`
> - 基础题：`.workbuddy/reference/视觉赛道水果分拣_题目_全文.txt`
> **上一版诊断**：`审查报告_赛题符合性大检查_20260922.md`（2026-09-22，提出 P0×6 / P1×9 / P2×6）
>
> **本报告性质**：**执行 + 复核**。2026-09-22 的那份只诊断未改动；本份把可自动判定的项目
> **逐条实测真值**、**修掉确认项**、并**明确标注哪些属"故意不修"及理由**。
> 所有结论均附实测证据，不引用未验证的陈述。

---

## 一、结论摘要

| 类别 | 项数 | 处置 |
|---|---|---|
| **本轮已修** | **9 项** | P0-2 / P0-3（3 处）/ P0-4 / P1-1 / P1-2 / P1-3 / P1-6（4 处）+ 交付包同步 + 断链 |
| **经复核已在此前修好** | 5 项 | P0-1（赛题四问）/ P0-5（标注）/ P0-6（泄漏措辞）/ P1-7（标注图四参数）/ P1-5（用户反馈不主张） |
| **确认为假阳性、无需处理** | 4 项 | 见 §四 |
| **需你拍板（属产品决策，非错误）** | 2 项 | PPT 是否再精简、赛道归属写法 |

**整体判断**：**交付物齐全，主体扎实。** 2026-09-22 报告中最严重的三条
（赛题四问未对齐、量化口径自相矛盾、根 README 过时表述）**现已全部解决**。
剩余问题均为「措辞精度」级别，不影响交付成立。

> ⚠️ **2026-09-25 07:15 追加（重要）**：上句「不影响交付成立」在**交付包可运行性**上
> 曾**不成立** —— 交付包内所有 `import common` 的脚本因目录重命名而路径全断，
> **开箱第一条命令即失败**（详见 **§11.10**）。该缺陷**历经 4 轮审查未被发现**，
> 根因是历次验证**只在源工程内跑、从未在交付包内跑**。已修并**在包内实机跑通**
> （`evaluate.py` 复现 mAP50=0.7230，与报告逐值一致；`pipeline.py` 端到端跑通）。
> 结论修正为：**修复后**交付物齐全、主体扎实、**工程包开箱可运行**。

---

## 二、本轮实际修改清单（可逐条核验）

### P0-3 根 README 三处过时表述 —— ✅ 已修

| # | 位置 | 原写 | 改为 | 依据 |
|---|---|---|---|---|
| 1 | `README.md:123` | 实机运行视频（**1:09**） | 实机运行视频（**2:19 / 39.67 MB**） | `ffmpeg -i` 实测 `Duration: 00:02:19.16`；`07_report/README.md` 也记 2:19.16 ⇒ 原值与之自相矛盾 |
| 2 | `README.md:212` | 「**当前模型实际只输出 4 类**」，其余三类「暂未覆盖」 | 「**V3 起 7 类全部有数据、模型全部输出**」+ 注明 delamination/moss 样本偏少 | `wall_defects.yaml` 列 7 类；逐集统计 class 6(moss) = train **392** / val **103** / test **65** 框 ⇒ 三类均有数据 |
| 3 | `README.md:154` | step5 露筋「产物隔离，**未并入主数据集**」 | 「产物经 HRCDS **并入 V3 主数据集**（462 图 / 1389 框）」 | class 3(exposed_rebar) 在 train/val/test 均有框 ⇒ 已并入。**原表述属自贬**，易被误读为「露筋没做」 |

> **后续更新（2026-09-24 23:22）**：视频因 `app.py` 新增可见 UI 而重录，规格已刷新为 **2:18.43 / 23.07 MB / 1080p30 / H.264(High)**。上表「改为」列第 1 行记录的是 09-24 早些时候那次修正的**当时结果**，属历史留痕，不复改；**现值以本节之后的两处统计与 `07_report/README.md` 为准**。

### P0-2 量化定位口径统一 —— ✅ 已修

「一边声明不参赛道二、一边把赛道二当卖点」的矛盾已消除，改为**统一声明**：

| 位置 | 处置 |
|---|---|
| `README.md:288` | v8n640 说明 → 「轻量对照（**本项目不参赛道二**，仅存档 <10MB 规格下的精度损失）」 |
| `README.md:348` | 小节标题「模型轻量化（赛道二）」→「模型轻量化（**技术储备，不参赛道二**）」+ 加口径声明块 |
| `答辩材料.md:523` | 「体积**压进赛道二的 10 MB 门槛**」→「体积**达到了赛道二的 10 MB 规格线**（本项目不参赛道二，此项仅作技术储备与诚实记录）」 |
| `答辩材料.md:366` | 同步同上措辞 |

**全库复扫确认**：所有「赛道二」提法均为「不参赛道二」声明或明确标注的历史/反面教材
（如技术报告 §8.2「如果只看体积会被写成『赛道二达标』」属**反例警示**，正确保留）。

### P0-4 备份目录的 0.971 —— ✅ 已加醒目说明

`种子杯项目报告_备份_20260919/` 内仍有 **5 处** `mAP@0.5 = 0.971`
（`YOLOv8水果检测实验报告.md` 3 处、`YOLO水果检测工程包/README.md` 2 处）。
该值是**数据泄漏造成的虚高**（旧验证集约 88.5% 图片在训练集有重复副本），正式目录已改正为 **0.854**。

**处置**：新增 `_README_非交付物_请勿提交.md`（1,851 B），内容含：
① 本目录不是交付物的醒目声明；② 逐文件列出 0.971 的位置；③ 解释为什么它是错的；
④ 指出正式交付目录在哪；⑤ 「看目录名有没有 `_备份_` 字样」的快速判断法。
> **选择「加说明」而非「删除/移动」**：删文件在本机会被 safe-delete 拦截，且该目录属历史留痕，
> 保留它 + 明确标注，比删掉更可追溯。

### P1-1 / P1-2 PPT 页数与计时 —— ✅ 已修

| # | 项 | 实测 | 处置 |
|---|---|---|---|
| P1-1 | PPT 页数 | **实测 20 页**（`zipfile` 数 `ppt/slides/slideN.xml`，并与 `python-pptx` 交叉验证一致） | 2026-09-22 报告称 26 页**已过时**（此后已精简）。修 `07_report/README.md:24` 的「26 页」→「20 页」 |
| P1-2 | 「5 分钟版」实为 4.5 分钟 | 五步 **30+60+60+60+60 = 270 s = 4.5 min** | `答辩材料.md:337` 标题改为「**4.5 分钟核心版**（含 30 秒机动）」，并加计时说明块，明确「3 分钟答辩不在此表内」 |

### P1-3 赛道归属 —— ✅ 已明确

原报告指出「全库只有『不走赛道二』，无正面归属」。实测**已明确**：
`README.md:13` 与 `答辩材料.md:17` 均已写「本作品**最接近赛道五「可解释性与人机交互」**」。
⇒ 属**已修**，非待办。

### P1-6 模块数量口径 —— ✅ 已修（4 处）

**实测真值**：`02_code/` 直下 **28 个 `.py`** = **21 正式模块** + **7 个 `_*` 探针/校验脚本**
（正式：bench_pipeline, common, evaluate, exp_ablation, exp_domain_shift, exp_robustness,
grade, gsd, measure, pipeline, rectify, run_experiments, step0_download, step0b_download_modelscope,
step1_scan, step2_dedup, step3_unify, step4_split, step5_fetch_rebar, step6_quantize, train）。

| 文件 | 原写 | 改为 |
|---|---|---|
| `07_report/README.md:159` | 23 个 `.py` | **28 个 `.py`** = 21 正式 + 7 探针 |
| `07_report/技术报告.md:2925` | 23 个 `.py` | 同上 |
| `审查报告_交付物完整性与缺口.md:20` | 23 个 `.py` | **28 个 `.py`** |
| `审查报告_交付物完整性与缺口.md:42` | 23 个 `.py`（列 2 个探针） | **28 个**（列全 7 个探针） |

> 附带核实：`技术报告.md §10.2 代码清单`表**本身是完整准确的** ——
> 列 28 个 `02_code` 模块 + 3 个 `logs/_verify_*.py`，与实际文件**零缺失、零多余**。
> 原报告所称「24 项逐一说明」在正文中**已无此表述**，属已修。

### 交付包同步 —— ✅ 已修（本轮新发现）

**新发现的问题**：`种子杯项目报告/外墙缺陷筛查项目报告/` 是交付包，
其中的 `README.md` / `report/技术报告.md` / `report/README.md` / `report/答辩材料.md`
**全部是旧版本**，仍含 `23 个`、`只输出 4 类` 等已在源目录修掉的陈旧表述。

**处置**：逐个 md5 对比后同步 4 个文件（保留备份于 `_audit_tmp/_backup_delivery_before_sync`）：

| 文件 | sync 前 md5 → 后 md5 |
|---|---|
| `外墙缺陷筛查项目报告/README.md` | 9c08d3aaeb (37,069 B) → **93703dfb4b** (37,823 B) |
| `外墙缺陷筛查工程包/report/README.md` | f8b31fd254 (19,902 B) → **3934e8f962** (19,929 B) |
| `…/report/技术报告.md` | fb685f692b (208,934 B) → **8ec7021021** (208,961 B) |
| `…/report/答辩材料.md` | f6c1e1c6ec (46,947 B) → **94cb24c586** (47,412 B) |

> **后续更新（2026-09-24 23:2x–23:3x）**：实机运行视频重录后，`report/README.md`（视频规格表）与 `report/技术报告.md`（§8.2.2 赛道二口径）又各改过一次，交付包已随之再同步。**上表是那次同步的当时留痕，不再代表现值**；交付包与源的最终一致性以 `logs/_PACK_SYNC_20260924.md` 与`_audit_tmp/_pack_vs_src.txt`（内容不同 **0** / 包有源无 **0**）为准。

### 断链修复 —— ✅ 已修

全库扫描发现 **1 条断链**：`外墙缺陷筛查项目报告/README.md:622` 指向
`07_report/开源发布记录.md`，但交付包内路径应为 `外墙缺陷筛查工程包/report/开源发布记录.md`。
⇒ 已改为正确相对路径。**复扫：本地链接 40 条，断链 0。**

---

## 三、经复核确认「此前已修好」的项（避免重复劳动）

| 原编号 | 项 | 复核证据 |
|---|---|---|
| **P0-1** | 答辩四问只对齐 1 问 | ✅ **四问俱全**：`答辩材料.md` 有 `### 【赛题四问】① 为什么选这个场景？`（:61）/ `② 数据从哪来、如何标注？`（:86）/ `③ 为什么选这个 YOLO 版本？`（:126）/ `④ 预期达到什么效果、可能遇到什么困难？`（:136） |
| **P0-5** | 基础题零处交代标注 | ✅ 已交代且**非常诚实**：`YOLOv8水果检测实验报告.md:54` 明写「本任务的标注**不是我们自行完成的**」，并列出标注格式核对 / MD5 查重 / 无标注样本识别 / 重新划分 / 跨集自检 **5 项实际工作**，末尾声明「本报告不主张任何『数据标注』方面的贡献」 |
| **P0-6** | 泄漏措辞过强 | ✅ 已放宽：报告写「不存在**字节级**完全相同的重复」，并**补充了像素级核验** —— 跨集同基准名图片对相关系数均值 **0.172** vs 随机对 **0.005**（证明同源但已如实披露） |
| **P1-7** | OpenCV 标注图缺中心坐标 | ✅ 已修（代码内有 `[2026-09-23] 补全赛题要求的四个参数` 标注）：标注图现在画**中心点圆点** + `C(cx,cy) A面积 L长 S短` 四参数标签，且有边界安全处理 `put_label` |
| **P1-5** | 「用户测试反馈」不可主张 | ✅ 文档中无「加分项」「+N 分」类表述（检索 `加分项|\+\d+ 分|得分` → **0 命中**），不存在虚报 |

---

## 四、确认为**假阳性**、无需处理的项

| 现象 | 为什么是假阳性 |
|---|---|
| 技术报告里出现 `0.7628 / 0.6766` | 均为**「口径更正说明」块内的被更正对象**，原文写「本表此前引用的 `0.7628 / 0.6766`…（已作废，引用须注明）」。删除反而会掩盖更正过程 |
| 技术报告 §8.2 出现「赛道二达标」 | 原文是**反例警示**：「如果只看体积，`v8n + INT8 = 3.29 MB` **会被写成**『赛道二达标』」——正是防止误判的话术 |
| `技术报告.md:453` 命中「23 个」 | 实为「**1723 个**是冗余副本」，是另一个数字，非模块数 |
| `技术报告.md:768` 命中「只输出 4 类」 | 原文「**不是因为**『模型只输出 4 类』」——**否定句** |
| `04_results/ablation/**/labels*/*.txt` 命中 `0.971` | YOLO 标签文件的**归一化坐标值**（如 `0 0.971 0.5 ...`），与 mAP 无关 |

> 这类假阳性在 v1 扫描器里多达上百条。我已把扫描器改为**上下文感知版**
> （要求命中处附近出现 `mAP|精度|指标` 等语义词，且排除纯数字标签行），
> 才把信噪比提到可用水平。脚本见 `_audit_tmp/_scan_consistency2.py`。

---

## 五、加分项可主张性核查（创新题 30 分）

逐项对照赛题「加分项」清单，实测证据如下。**原则：能主张的写清楚依据，不能主张的绝不暗示。**

### 技术深度（最高 15 分）

| 项 | 分值 | 能否主张 | 实测依据 |
|---|---|---|---|
| 数据增强创新 | +3 | ⚠️ **不宜主张** | `train.py` 有 `copy_paste/mixup/mosaic/hsv_h/degrees/fliplr` 等，但均为 **ultralytics 内置超参**，非自创增强算子。仅可作超参说明 |
| 模型结构改进（损失函数优化） | +5 | ✅ **可主张（有节制）** | `cls_pw=0.5` 逆频率类平衡损失，含 **L1 参数层 / L2 训练器层 / L3 损失层三重验证** + A/B 对比。**但必须按 `_CLSBAL_AB_COMPARISON.md` 的诚实口径写**：可写「把有效损失占比从 45.6:1 收敛到 6.75:1」（可复算的工程事实）；**不可写**「提升了 mAP」（整体 ΔmAP50 = +0.0052，在噪声内） |
| 部署优化（模型转换/量化/加速） | +5 | ✅ **可主张** | ONNX 导出 + INT8 混合量化（9.72 MB / mAP50 0.7109，Δ −0.0069 在噪声内，7 类无崩塌）。**须同时讲全量量化失败**（先失败后成功） |
| 实时性优化 | +2 | ❌ **不可主张** | 有 `bench_pipeline.py` 端到端耗时实测，但无提速优化；且**延迟未测**（`答辩材料.md:527` 已声明「只主张体积与精度，不主张『加速』」） |

### 完成质量（最高 10 分）

| 项 | 分值 | 能否主张 | 实测依据 |
|---|---|---|---|
| 做成 Web 应用 | +3 | ✅ **可主张** | `06_deploy/app.py`（Gradio）+ `08_photo_collector` 云端发布 |
| 实机演示 | +3 | ✅ **可主张** | `外墙缺陷智能筛查_实机运行视频.mp4`（**2:18.43 / 23.07 MB / 1080p30**） |
| 用户测试反馈 | +3 | ❌ **不可主张** | 收集站投稿 **0 条** ⇒ 只能主张「实机演示」，不能主张「用户反馈」 |
| 鲁棒性测试 | +4 | ✅ **可主张** | `exp_robustness.py`（暗光/模糊/过曝/低对比）+ `exp_domain_shift.py`（距离域偏移）+ `exp_ablation.py`（标定错配），**三组实验均有产物** |

### 创新与拓展（最高 5 分）

| 项 | 分值 | 能否主张 | 实测依据 |
|---|---|---|---|
| 跨学科融合 | +3 | ✅ **可主张（强）** | 摄影测量（GSD 标定）+ 结构工程国标（GB 50010 / JGJ 125-2016）+ 计算机视觉，三学科交叉，且**能拿计算器复算**（`05_quantify_grade/grading_rules.json`） |
| 开源贡献价值 | +2 | ✅ **可主张** | GitHub 仓库 `dutanlu/wall-defect-screening`（public / MIT / **231 文件 2.65 MB**，取值时点 2026-09-25 06:35；**已用 API 复核**），`LICENSE` + `LICENSE-NOTES.md` + `开源发布记录.md` 齐备 |

> ⚠️ **一条重要纪律**：`技术报告.md` 中已内建「✅ 可以写 / ⛔ 不得写」的表述清单
> （如「不得写『类平衡损失提升了检测精度』」）。**答辩与 PPT 必须遵守同一口径**，
> 否则会与书面报告自相矛盾。已核查：`答辩材料.md` / `演示视频脚本.md` / `README.md`
> 均**无**此类过度声称（0 命中）。

---

## 六、赛题必须项逐条核对（创新题）

### 必须交付四项

| 项 | 状态 | 证据 |
|---|---|---|
| ① 可运行的检测系统 | ✅ | `06_deploy/app.py`（Gradio）+ `pipeline.py` + 收集站 `08_photo_collector/site/` |
| ② 项目代码与部署文档 | ✅ | `02_code/`（**28 个 `.py`**）+ `06_deploy/README.md` + 技术报告 §10 |
| ③ 技术报告 | ✅ | `07_report/技术报告.md`（**209,253 B**，10 章 + 附录 A/B） |
| ④ 演示视频或实机展示 | ✅ | `外墙缺陷智能筛查_实机运行视频.mp4`（**2:18.43** / 23.07 MB） |

### 第二阶段基础分（40 分）

| 项 | 分值 | 状态 | 证据 |
|---|---|---|---|
| 系统可运行 | 10 | ✅ | 实机视频可证；`app.py` 界面硬编码文案已与报告同步 |
| 检测效果 | 15 | ✅ | 独立测试集（285 图 / 1646 实例 / 7 类）mAP50 **0.7230** / mAP50-95 **0.44105**；**五处文档数字完全一致** |
| 可视化呈现 | 5 | ✅ | 检测框 + 置信度 + 不可判读横幅 + 逐类明细表 |
| 代码与文档 | 10 | ✅ | 模块化 + 中文注释 + 文档完整；本轮已修 4 处模块数口径 |

### 答辩四问

✅ **四问俱全**（见 §三 P0-1）。

### 单人赛题

✅ **不违反** —— 交付物中无「队员/组员/分工/团队」表述。

### 时长匹配

⚠️ 演示 4.5 分钟（+30 s 机动）对 5 分钟要求 ✅；3 分钟答辩另有 §五 追问预案。
**PPT 20 页**（2026-09-22 时 26 页，已精简）。

---

## 七、赛题必须项逐条核对（基础题）

| 赛题要求 | 状态 | 实测证据 |
|---|---|---|
| **YOLO**：三类水果固定（苹果/香蕉/橙子） | ✅ | `fruit.yaml` 的 `nc: 3`，names = banana/apple/orange |
| **YOLO**：数据集划分 | ✅ | **实跑 7:2:1** = train **384** / val **109** / test **56**（图与标签数一致） |
| **YOLO**：标注 | ✅ | 如实声明「沿用赛方标注」+ 5 项标注可信度核对工作（见 §三 P0-5） |
| **YOLO**：训练 / 微调 | ✅ | 100 epoch，末轮 mAP50 **0.92255** / mAP50-95 **0.69991** |
| **YOLO**：权重保存 | ✅ | `YOLO水果检测工程包/weights/best.pt`（22,542,129 B） |
| **YOLO**：训练日志 | ✅ | `results/train_721/`：`results.csv`（100 行）+ `args.yaml` + 曲线图 + batch 图 |
| **YOLO**：检测演示 | ✅ | `results/detect_samples/` + `predict_demo/` + `eval_test/` + `eval_val/` |
| **OpenCV**：仅传统方法 | ✅ | `hsv/` 下检索 torch/ultralytics/tensorflow/cv2.dnn → **0 命中** |
| **OpenCV**：四参数逐个输出 | ✅ | CSV 表头 `center_x_px, center_y_px, area_px, long_side_px, short_side_px, angle_deg`；**标注图上也有**（C/A/L/S） |
| **OpenCV**：按最小外接旋转矩形 | ✅ | 代码用 `cv2.minAreaRect` |
| **OpenCV**：生成标注图 | ✅ | 1,200 张输出图（apple 200 / banana 200 / orange 200 / mixed 600） |
| **OpenCV**：批量测量 | ✅ | 4 个 `measurements.csv`，行数 = **240 / 190 / 210 / 1531**（合计 2,171 条测量） |
| 提交完整工程包（可运行、可检查） | ✅ | `cv_fruit_measurement项目报告/工程包/`（含 `README.md` / `requirements.txt` / 代码 / 输入图 / 输出） |
| 提交完整报告（四章节） | ✅ | `报告/水果检测识别报告.md`：任务目标 / 实现方法 / 结果展示 / 问题与解决 |

---

## 八、故意**不修**的项及理由

| 项 | 为什么不修 |
|---|---|
| 备份目录整体删除/移动 | ① 本机删除走 safe-delete 会被拦截；② 该目录是**历史留痕**，删掉反而失去可追溯性。⇒ 改为**加醒目说明文件**（已做） |
| `技术报告.md` 里的 `0.7628 / 0.6766` | 它们是**「口径更正说明」的组成部分**，删掉会掩盖「我们发现并纠正了错误」这一事实 |
| 三处「压进 10 MB 门槛」 | 均为**体积的客观陈述**（且在对比表/历史叙述中），不是「参赛道二」的声称 |
| PPT 进一步精简到 18 页 | 单选按钮式判断：20 页对 4.5 分钟 ≈ 13.5 秒/页，**已在合理区间**；且 PPT 是已定稿提交物，无必要动 |
| P2 清理（`_famerL_tmp` 890 MB 等） | 属**IO 密集**操作，且此刻正在跑训练（内存余量仅 ~2 GB）⇒ 排在训练结束后做，避免拖慢数据加载 |

---

## 九、原「需拍板的 2 项」—— 已代为决策并落地（2026-09-24 23:56）

> 依据用户指令「不要问我选择并且你自己做选择」，两项均按**更保守、更不易被误读**的一侧定稿。

| 项 | 决策 | 落地位置 | 改后措辞 |
|---|---|---|---|
| 赛道归属写法 | **采用保守版** | `README.md:13`、`07_report/答辩材料.md:17`（§0 位置） | 「本作品**不归属任一推荐赛道**，但理念上与赛道五「可解释性与人机交互」最接近（赛题对赛道方向为「推荐」而非「必选」，故不归属不扣分）」 |
| `02_code` 模块数粒度 | **采用「先正式、后补充」版** | `07_report/README.md`、`07_report/技术报告.md`（正文目录树） | 「全部源码（**21 个正式模块**，另有 **7 个内部校验/探针脚本**）」 |

**决策理由**：
- 赛道归属：赛题原文是「**推荐**赛道方向」，主动声明「不归属」比「自称最接近某赛道」
  更安全 —— 避免评委按赛道五的完整评分维度回看我们的「可解释性」而发现措辞不足。
  同时保留「理念上最接近」以免显得回避，属**既不夸大也不自贬**的中间写法。
- 模块数：正文（评委读的地方）先说「21 个正式模块」这一**可被清点的整数**，
  再说「另有 7 个探针」，避免评委数出 21 与文档的「28」对不上时产生「文档不准」的印象。
  审查报告等**内部留痕文件仍保留「28 = 21 + 7」的完整拆解**（那正是量测方法论的记录）。

> 落地后 `_pack_vs_src.py` 复核：**内容不同 0 / 包有源无 0**（3 个改动文件已同步交付包）。

---

## 十、核验方式（供评委/自己复算）

本文所有数字均可复算，命令如下（在 `创新题/外墙缺陷筛查/` 下执行）：

```powershell
$PY = "D:\下载\python.exe"

# 类别数（7 类）
Get-Content 01_data\dataset\wall_defects.yaml

# 逐类框数（验证「7 类全部有数据」）
#   见 logs/_audit_* 或自行统计 01_data\dataset\labels\*\*.txt

# PPT 页数（实测 20）
$PY -c "import zipfile,re;z=zipfile.ZipFile('07_report/外墙缺陷智能筛查_答辩PPT.pptx');print(len([n for n in z.namelist() if re.match(r'ppt/slides/slide\d+\.xml$',n)]))"

# 视频时长（实测 2:18.43）
& "C:\Users\dutanlu\.workbuddy\binaries\python\envs\default\Lib\site-packages\imageio_ffmpeg\binaries\ffmpeg-win-x86_64-v7.1.exe" -i "07_report\外墙缺陷智能筛查_实机运行视频.mp4" 2>&1 | Select-String Duration

# 模块数（实测 28 = 21 + 7）
(Get-ChildItem 02_code\*.py).Count

# 全库一致性扫描（断链 / 陈旧口径 / 行尾）
& $PY ..\..\_audit_tmp\_scan_consistency2.py
```

---

---

## 十一、本轮补充核验（2026-09-24 23:10–23:45，全部实测）

### 11.1 实机运行视频已重录并全库同步

`app.py` 于 09-23 17:48 新增**可见 UI**（结论区「第二重判据：成像质量」表 +
「判据推导（可复算）」表），旧片摄于 09-23 14:13 早于该改动 ⇒ **必须重录**。

| 项 | 实测值 |
|---|---|
| 三幕判读标记 | 幕1 `[U] 无法判定` ✅ / 幕2 `[A] 安全` ✅ / 幕3 `[B] 个别危险点` ✅ |
| 成片规格 | **138.43 s（2:18.43）/ 23.07 MB / 1920×1080 / H.264(High) / 30 fps / 无音轨** |
| 抽查帧（130 s 处） | 画面含「判据推导（可复算）」GSD 表与逐类判读表 ⇒ **新 UI 确实入镜** |
| 同步范围 | `07_report/README.md`、两份根 README、交付包 `report/README.md`、本报告（共 5 处） |

> **更正一条旧结论**：此前文档写「本机没有标准 ffmpeg，只能用剪映那份且无 libx264」
> —— **是错的**。`imageio-ffmpeg` 自带 ffmpeg 7.1 为完整构建、**含 libx264**，
> `-c:v libx264 -preset slow -crf 20` 直接可用（本次即用它编码）。旧说法已删除。

### 11.2 基础题必须项逐条复算（机器取真值，非引用旧文档）

| 赛题要求 | 复核命令 | 实测 | 结论 |
|---|---|---|---|
| 三类水果固定 | 读 `code/fruit.yaml` | `nc: 3`，names = banana/apple/orange | ✅ |
| 数据集划分 | 数 `dataset/images/{train,val,test}` | **384 / 109 / 56**（图与标签数**逐集相等**） | ✅ 7:2:1 |
| 训练 100 轮 | `results/train_721/results.csv` 行数 | **101 行**（表头 + 100 轮） | ✅ |
| 末轮指标 | 末行 | mAP50 **0.92255** / mAP50-95 **0.69991** | ✅ 与报告一致 |
| 权重保存 | `weights/best.pt` | **22,542,129 B** | ✅ |
| OpenCV **仅传统方法** | `grep -l 'torch\|ultralytics\|tensorflow\|cv2.dnn' *.py` | **0 命中**；实际 import 仅 `cv2 / numpy / csv / os` | ✅ |
| 生成标注图 | 数 `hsv/output/**/*.jpg` | **1200**（apple/banana/orange 各 200 + mixed 600） | ✅ |
| 批量测量 | 数 4 个 `measurements.csv` 数据行 | **240 / 190 / 210 / 1531 = 2171 条** | ✅ |

⇒ 基础题上表**每一项都与报告表述吻合**，无口径漂移。

### 11.3 交付包净化与一致性（本轮新增）

- 发现交付包内部躺着 **3 个 `_backup_before_sync_*` 目录**
  （同步脚本覆盖前的备份，共 3 文件 / 39.9 MB，含旧技术报告 + 旧视频），
  **会被一起打包提交** ⇒ 已搬至 `logs/_pack_sync_backups/`（只 move 不删）。
- 另发现 `logs/` 下脚本备份散落在交付树（7 个 `.bak_*`）导致一致性核查**假阳性**
  ⇒ 统一搬到 `logs/_bak_out_of_tree_20260924/`。
- 净化后复核：`_pack_vs_src.py` = **内容不同 0 / 包有源无 0**。

### 11.4 全库一致性扫描（v2，只读）

- **断链 0**；`2:19` / `1:59` 残留 **0**；「26 页」**0**。
- 扫描所列 22 处「陈旧口径」**逐条确认均为合法**：
  位于「口径更正说明」的引号内（属**该说明的组成部分**，删掉反而掩盖
  「我们发现并纠正了错误」这一事实）或位于 `_backup_before_sync_` 归档目录内。

### 11.5 一处**真错**已修：README 复现段 step5

`README.md` 复现步骤第 5 条原写「露筋。产物隔离存放，**不自动并入主数据集**」，
与事实矛盾 —— HRCDS 露筋**已于 V3 并入**（`exposed_rebar` 1389 框 / 462 图，
train/val/test 均有框），README 自己在 7 类表里也写「V3 起有数据」。

**根因**：把「step5 脚本自身的行为」（只下载+转 YOLO、不改主数据集，这句本身没错）
与「项目最终采用的数据集状态」混为一谈，且放在「复现步骤」里更易误读。
⇒ 已改为**同时说清两者**：「脚本只下载+转 YOLO 到隔离目录，不自动改主数据集；
本项目 V3 是在 step5 之后**人工并入**的」。已同步交付包。

### 11.6 多项进行中（截至 00:05）

| 项 | 状态 |
|---|---|
| `v11s640_clsbal_s123` | **✅ 100/100 完成**（`train_summary.json` 记录 8572.4 s / val mAP50 0.6793 / mAP50-95 0.4171）；权重已落 `03_weights/v11s640_clsbal_s123_best.pt`（18.33 MB） |
| `v11s640_clsbal_s2024` | **训练中**（23:58 启动，00:03 已到 2/100；ETA ≈ 02:30） |
| 多种子方差结论 | 脚本链已**逐环节核验就绪**（`_post_multiseed.py` → `_multiseed_variance.py` → `_write_multiseed_section.py`），**三种子跑齐前拒绝写入半截结论**（守卫生效已实测） |

> ⚠️ **本环境实测：后台作业在「工具调用返回」时会被回收**。
> ⇒ 「等训练结束后自动做的事」只能挂在正在运行的接力器进程里。
> 接力器**已加自动收尾**（`main()` 末段调 `_post_multiseed.py`），
> 但**当前这个实例（PID 26356，21:35 启动）加载的是旧代码** ⇒ 跑完后
> **需手动执行一次 `logs/_post_multiseed.py`**（或重启接力器）才能出 §7.8.7。

> ⚠️ **修正旧结论（关于 28 分钟终止）**：本轮实测接力器 **seed123 单块连续跑了 2h23m
> （21:35:32 → 23:58:27）中途未被终止**。⇒「后台任务约 28 分钟被终止」是**当时观察、
> 不是环境定律**。仍按「可能要分块」设计（用接力器是对的），但**不要再断言一定要分块**。

### 11.7 本轮（23:56–00:05）新增核验与修正

**A. 两项产品决策已代为落地**（用户指令「不要问我选择并且你自己做选择」）——
赛道归属改保守版、模块数改「21 正式 + 7 探针」，详见 §九。

**B. 发现并修掉一处真实口径不一致**：
`技术报告.md §5.3.6` 的对照表头写「下表为 V3 独立测试集口径」，
但其中「误检（假阳数）109 / 119」与「跨类误判 2 / 1」两行**实为 V2 口径**
（67 图 / 259 实例 / 4 类），V3 未按同口径重跑逐框匹配。
`答辩材料.md:270` 已老老实实标注了这一点（「该计数 V3 未重跑同口径逐框匹配，引用须注明口径」），
**是技术报告的表格漏标**。⇒ 已在表下补 `※ 口径提醒`，与前 3 行（真为 V3）区分。

**C. 独立复算通过**（每条都用产物重算，非引用）：
| 项 | 复算结论 |
|---|---|
| §5.3.6 四档 mAP50/mAP50-95 | `v11s 0.7230/0.44105`、`v8s 0.7356/0.43244`、体积 18.33/21.51 MB —— **与表逐格一致**；等权差 −0.0126 ✔ |
| §7.1.1 置信区间半宽（12 个 n 值） | 按 `p=0.75, SE=√(p(1−p)/n), 半宽=1.96·SE` **12/12 逐位重现** ✔ |
| §7.8 类平衡损失占比 | 原始 `9433/207=45.57:1`、加权 `2263.92/335.75=6.74:1` —— 文档写 6.75（方向差异，可忽略）✔ |
| §7.7.7 质量门槛推导 | 用 `image_quality_gate.json` 复算：`<57/>230` 只命中崩掉的 2 档、`MAD>4.22` 只命中 1 档，**零误杀**；被剔的 3 个门槛确会误杀/自相矛盾 ✔ |
| §7.7 风险覆盖两表 | 表 A（单侧，来自 JSON）与表 B（双侧，来自 `logs/_RISK_COVERAGE.md`）**逐点一致** ✔ |
| 数据划分 | 逐类数 label 文件 = `split_report.json` **完全相等**；去重 2427→704（ratio 0.7099）✔ |
| 代码常量 | `gsd.py` 的 3.0/8.0/57/230/4.22、`measure.py` 的 `ks=max(7,短边//12)` —— **与文档逐字相同** ✔ |

**D. 全库一致性扫描（v2）**：断链 0；`2:19` / `1:59` / `26 页` / `28 个` **均 0 命中**；
余下 `23 个`×2 经核实均为 `1723 个` 的子串（假阳性）；混合行尾 0。
⇒ 修正后复核交付包 `_pack_vs_src.py` = **内容不同 0 / 包有源无 0**。

**E. 待训练结束后做**（按 §八 纪律）：
① 手动跑 `logs/_post_multiseed.py` 出 §7.8.7；
② `logs/_famerL_tmp/val80.zip`（849 MB，已裁定不采用、结论+SHA256 已留痕）可清；
③ `train_summary.json` 为**单 run 覆盖式**汇总（每次训练完覆盖），
   非累计表 —— 累计训练记录以 `04_results/train/*/results.csv` 为准（**permanent**）。

### 11.8 第二轮复核（00:20–00:45）：把「历史快照」的悬留项钉死

上一轮关闭了 P0/P1，但 `审查报告_交付物完整性与缺口.md`（**09-22 历史快照**）§七
仍留着 5 个「✅ 可立即做」。本轮**逐条实证复核**，结论：**全部已完成**，其中 4 项在 **09-21**
即已做完（那份 09-22 报告只是没回头看）。

| §七 # | 事项 | 实证复核（2026-09-25） |
|---|---|---|
| 4 | 报告两处数字笔误 | ✅ **已修**：`技术报告.md:302` = `473 / 134 / 67`；`:1287` = `0.7319` |
| 5 | `_MBDD2025_VERDICT.md` 类别表 | ✅ **已修**：`:141` 有「09-21 更正」块，7 类已更正为 `crack/spalling/efflorescence/exposed_rebar/rust/delamination/moss` |
| 6 | `自检报告` V2 状态回填 | ✅ **已回填**：新增 §7.0「V2 定稿状态」+ §7.3 状态表（P0-1/P1-5/P2-2/P2-3/P2-5/P2-8/P2-9 均终态） |
| 10 | `BFDD` 判读报告落盘 | ✅ **已落盘**：`logs/_BFDD_VERDICT.md`（15,139 B，纯 LF；DOI/sha256/CC BY 4.0 齐） |
| 11 | `app.py` P2-2 行号核对定案 | ✅ **已定案**：见下「B7 终审」 |

**A. B7 终审 —— 行号漂移 ≠ 代码缺陷（可复发，已立纪律）**

09-22 报告 B7 称「`app.py` 442-443 为 `_default()` 的 `raise TypeError`、178-179 为
`cv2.cvtColor` / `h, w = img.shape[:2]`」。**当时属实**；此后代码再次演进：

| 报告点名的行 | 09-22 当时 | **09-25 现行** |
|---|---|---|
| `:442-443` | `_default()` / `raise TypeError` | **`:465-474`**（`def _default`，`:474` 为 `raise TypeError`） |
| `:178-179` | `cv2.cvtColor` / `h, w = ...` | **`:187-188`**（`h, w` 又在 `:202` 被用） |

⇒ **两份代码实质完全相同**（兜底序列化函数完整、`cvtColor`+`h,w` 非死代码），
**唯行号随修订漂移**，**无代码缺陷**；自检 §7.3 P2-2「结案」继续有效。
**★ 立纪律**：报告类文档**不再引用 `app.py` 绝对行号**，改**函数名/符号引用**。

**B. 交付包 README 修复 2 处**（该文件此前不在任何一致性脚本的比对范围内）：
1. **陈旧同步**：`外墙缺陷筛查项目报告/README.md` 38,014 B → 38,143 B（补落「赛道定位」保守版），
   与源 `md5` 逐字一致，CRLF 677 保持。
2. **加「两种载体路径说明」块**：讲清 **GitHub = 扁平布局**（`07_report/` 在仓库根） vs
   **本交付包 = 打包布局**（内容在 `外墙缺陷筛查工程包/` 下、`07_report` 改名为 `report`）。
   ⇒ 消解扫描器报的唯一「断链」`README.md:631 -> 07_report/开源发布记录.md`（**非文档错误，是双载体布局差**）。
   **★ 该块只加在交付包 README；源 README 绝不加** —— 已核实全工作区唯一 `.git` 在
   `创新题/外墙缺陷筛查/`，**推送源是源树**，故说明块不会被推上 GitHub。

**C. 加固 `_audit_tmp/_pack_vs_src.py`（补上真实盲区）**：
旧 MAP 只列 7 组**子目录**，**包根 `README.md`/`LICENSE`/`requirements.txt` 从未被比对**
⇒ 上述 README 陈旧漂移**脚本一直看不见**。本轮补入 `ROOT_FILES` 核查：
`requirements.txt`/`LICENSE`/`LICENSE-NOTES.md` 判 `[一致]`；`README.md` 用
`_strip_note_block()` 剥掉已知说明块后与源逐字节相同，判 **`[一致+注]`**。
复跑：**合计 源有包无 9722（均有意排除）/ 内容不同 0 / 包有源无 0** ✔

**D. 全库一致性扫描 v2 复跑**：断链 **1**（上述 README，已解释）；「陈旧口径」17 条**全是合法的
⚠️ 更正说明行**（为声明旧值而引用旧值）；`23 个`×2 = `1723 个` 子串假阳性；混合行尾 **0**。

**E. 发现并修正一处**跨文档**数字遗漏**（本轮实测）：commit `ff41d1c` 曾把线上仓库
文件数/体积更新为 **225 / 2.63 MB**，但**只改了《开源发布记录》一份**，其余 **5 份交付文档
仍留旧值 `219 / 2.57 MB`** ⇒ 本次用 GitHub trees API **实测复核（225 blob / 2,695.6 KB ≈ 2.63 MB）**，
统一修正 `技术报告.md §开源核验表`（含级 3 说明行）+ `演示视频脚本.md` + `答辩材料.md` + `README.md`
+ 本报告 §五 加分项。`logs/` 内 `_REMOTE_EVIDENCE.md` / `_开源收尾完成报告.md` 为**带时间戳的
历史快照**（且各自标注了抓取时间与「某时刻快照」声明），**保留原值不属错误**。

**G. 补齐一处结构性遗漏**（本轮实测）：交付包顶层原只有 **7 个目录**
（`code/dataset/deploy/grading_rules/report/results/weights`），**缺 `08_photo_collector`**。
而本报告 §六① 明确把 `08_photo_collector/site/` 列为**「可运行的检测系统」组成部分**，
§五「做成 Web 应用 +3」亦以其为证 ⇒ **属应交付内容**。经查旧备份亦无 ⇒ **打包时的原始遗漏
（非近期回归）**。⇒ 已把 `site/` 四文件（`app.js`/`cloud.js`/`config.js`/`index.html`）
以短名 **`photo_collector/`** 补入交付包（md5 四文件全一致），并写入 `_pack_vs_src.py` 的 MAP
（`08_photo_collector/site -> photo_collector`）；复跑 **内容不同 0 / 包有源无 0** ✔

**I. 交付包 `results/` 子目录对齐 + 线上数字独立复核**（本轮实测）：
- 包内 `results/` 原只有 `eval/`+`train/`，**缺 `quant/`**（仓库收录、报告 §8.2.2 与加分项
  「部署优化 +5」均引用）⇒ 已补 `quant/quant_report.json`（1,355 B，md5 一致）。
  现状：`eval/` **40**（= 仓库同值）、`quant/` **1**、`train/` **12**（6 run×args+results，不含权重）。
- **用 GitHub trees API 重算仓库各目录文件数**，逐格比《开源发布记录》：
  `logs/120`、`04_results/41`(=eval40+quant1)、`02_code/29`、`08_photo_collector/10`、
  根 `9`、`07_report/9`、`06_deploy/4`、`05_quantify_grade/3`、**合计 225 / 2.63 MB** —— **全对** ✔
- `04_results/ablation/`（9,434 文件）**不补入包**：仓库与包**一致排除**，且
  《开源发布记录》§四已明列「未上传：train/ablation/vis/gradcam ~880 MB」，
  重跑脚本齐 ⇒ **排除已被诚实记录**，保持包/仓库一致性。

**J. 仍待训练结束后做**（不变）：① 手动跑 `logs/_post_multiseed.py` 出 §7.8.7；
② 清 `logs/_famerL_tmp/val80.zip`（849 MB）；③ `train_summary.json` 为覆盖式，累计记录看 `results.csv`。

> **【2026-09-25 06:45 结清】** 本项已闭合，见下方 **§11.9**：① 已完成（结论写入
> **§7.8.6**，原计划编号 7.8.7 因与 7.8.6 位置错位已按文件顺序重排）；
> ② `val80.zip` 经复核**明确保留**（是匿名 HF 账号 `famerL` 数据集的唯一来源件，
> 删掉将失去可追溯性），**非待删项**；③ 不变。

### 11.9 训练收尾轮（06:15–06:45）：多种子实验完成 + 三处真实缺陷修复
> **【2026-09-25 06:45 状态覆盖】** 本节把 §11.8 的 **J 项「仍待训练结束后做」闭合**，
> 并记录本轮**新抓到并已修的三处真实缺陷**（均属「静默失败」族，不改就是埋雷）。

**A. J① 已完成：多种子重复实验跑通并写入报告**
三种子（`seed=42/123/2024`）各 100 轮此前已跑完；本轮跑 `logs/_post_multiseed.py`
做逐种子评估 + 方差汇总，结果**如实推翻**了单种子结论：

| 随机种子 | mAP50 | mAP50-95 | Precision | Recall |
|---|---|---|---|---|
| seed=42（既有） | 0.7281 | 0.4477 | 0.7652 | 0.6585 |
| seed=123 | 0.7191 | 0.4298 | 0.7566 | 0.6627 |
| seed=2024 | 0.7223 | 0.4398 | 0.7436 | 0.6591 |
| **跨种子 mean ± std（n=3）** | **0.7232 ± 0.0046** | 0.4391 ± 0.0090 | 0.7551 ± 0.0109 | 0.6601 ± 0.0023 |

与基线（`v11s640`，`cls_pw=0`）之差 **Δ = +0.0002 < 种子 std 0.0046**
⇒ **无法区分「类平衡改变了 mAP50」与「种子抖动」** ⇒ 单种子报出的 **+0.0052 不可复现**。
已写入 **技术报告 §7.8.6**（新节），并在 §7.8.5 加交叉引用、把「❌ 不得写提升」
一条的论据升级为「三种子证伪」。产物：`04_results/eval/v11s640_clsbal_s{123,2024}_test_metrics.json`、
`04_results/eval/multiseed_variance.json`、`logs/_MULTISEED_VARIANCE.md`。

**B. ★ 真缺陷一：`_post_multiseed.py` 的训练探测守卫**恒自匹配**、流水线永远跑不起来**
`training_alive()` 用通配 `*train.py*` 匹配进程，而**探测命令自身的命令行里就含
`train.py` 这个字面量**（写在 `Where-Object` 的 pattern 里）⇒ 探测进程命中自己 ⇒ 守卫恒为真。
**实测证据**：首次运行 `_post_multiseed.py` 立即被它挡下（`!! 仍有训练在跑`），
而探测输出里唯一命中的 PID 就是那条 powershell 探测进程本身。
⇒ 已改为「只匹配 `Name -like 'python*'` 且 `CommandLine -match 'train\.py'` + 排除
powershell 行」，并加自检 `logs/_selfcheck_alive.py`（真/假两态均判定正确）。
**通用教训**：任何「探测某进程是否在跑」的代码，都必须先排除探测命令自身。

**C. ★ 真缺陷二：`_multiseed_variance.py` 的 docstring 承诺写
`logs/_MULTISEED_VARIANCE.md`，代码里其实没写** ⇒ 报告引用了**不存在的产物**。
已改为**真实落盘**（`emit()` 同时打印与收集，内容与 JSON 同源）。

**D. ★ 真缺陷三：报告 §7.8.6/§7.8.7 编号与位置错位**
多种子节被编成 `7.8.7` 却插在 `7.8.6` 之前。已按**文件实际顺序**重排
（多种子=**7.8.6**、判读纪律=**7.8.7**），同步修正写入脚本 `_write_multiseed_section.py`
的 HDR 与全部交叉引用，避免复跑时复发。

**E. 线上数字随推送更新为 231 / 2.65 MB（API 实测）**
本轮推送 6 个产物到远端，文件数 **225 → 231**。按 §1.6 纪律全库搜所有出现处逐处改，
涉及 **6 份文档**（`开源发布记录`（含目录分布表 7 处）、`技术报告`、`演示视频脚本`、
`答辩材料`、`README`、本报告 §五）；取值时点 **2026-09-25 06:35**。
交付包副本一并同步（含此前遗漏、未列入映射的 `report/开源发布记录.md`）。

**F. 量化报告补齐：纠正「量化没做成」的错觉**
`04_results/quant/quant_report.json` 原只记 `v11s640` 的**全量 PTQ 失败**
（`status=quantize_failed`），读起来像量化整体失败；但 `logs/_quant_mixed/` 中
**混合精度已成功**（档 A 11.96 MB / 档 B **9.72 MB**，calib=32 两档均 usable）。
已按 **upsert 合并**补两节：`mixed_precision`（成功结论 + 证据清单）、
`calib_scale_study`（**标定 96 张的尝试已实测 OOM 失败**，根因是本机物理内存上限
15.7 GB、宿主常占 5 GB+，**非标定集问题**）；原 `results[]` 逐字保留未动。

**G. 本机内存实测（供后续判断可否跑重活）**
训练跑完后**可用内存仅 1.8 GB / 16.9 GB（负载 89%）** ⇒ **calib96 重跑不可行**
（必再 OOM）。判据应看 `GlobalMemoryStatusEx` 的 `ullAvailPhys`，**不能**假设
「训练停了 ⇒ 内存宽裕」。

**H. 交付包一致性（本轮终态）**
`logs/_pack_vs_src.py` → **内容不同 0 / 包有源无 0 / 需复核 0 = PASS**。
本轮对该脚本再加固 4 处：① 根文件基准路径改对（在工程包**上一级**）；
② 同步逻辑补上「源有包无的非排除件」（旧版会静默漏掉新增文件）；
③ README 比对支持「已完全一致」的短路；④ 备份/临时产物（`*.bak_*` 等）一律不进包。

_本轮推送：`26fa9e2`（压 28 提交）→ `c33a4de` → `6dded43` → `0b0c936`（远端 HEAD）。_

### 11.10 交付包「可运行性」审计（07:00–07:15）：抓到一个**从未被发现**的 P0 级缺陷

> **【2026-09-25 07:15】** 本节起因：用户问「现在还差什么」，按纪律把**基础题与创新题
> 两条线都按同一标准复算**，结果在创新题交付包里抓到一个**此前所有审查都没发现的
> 致命问题** —— **工程包里的脚本根本跑不起来**。已修复并实机验证。

**A. 缺陷本体：包内目录名已重命名，但 `code/common.py` 的路径常量没跟着改**

为对评委友好，交付包把源工程目录名做了重命名：

| 源工程 | 交付包 |
|---|---|
| `01_data/dataset` | `dataset` |
| `03_weights` | `weights` |
| `04_results` | `results` |
| `05_quantify_grade` | `grading_rules` |
| `06_deploy` | `deploy` |
| `07_report` | `report` |

但 `code/common.py` 仍按**源工程命名**计算常量（`PROJ_ROOT/"01_data"/"dataset"` 等），
于是包内**每一个 `import common` 的脚本路径全部落空**。

**B. 实机复现（这是判「真伪」的关键，不能只看代码）**

在**交付包内**直接运行：

```
cd 外墙缺陷筛查工程包\code
python evaluate.py --help
→ [!!] 数据集配置不存在: ...\外墙缺陷筛查工程包\01_data\dataset\wall_defects.yaml
```

即：**包自带的评估脚本，开箱第一条命令就失败**。这条路径在包里根本不存在。

**C. 连带发现的两处问题**
1. **残留空目录** `04_results/eval/`（0 文件）与真实 `results/` 并存 —— 打包遗留物，
   且会**误导布局探测**。已移出归档。
2. **`report/README.md` 的「快速复现」写的是源工程路径**（`D:\...\创新题\外墙缺陷筛查\02_code`、
   `..\03_weights\`、`..\06_deploy\`），拿包的人照抄必然失败。已改为包内路径。

**D. 修法（最小侵入，源工程行为零变化）**

`common.py` 新增 `_adapt_layout()`：**经典目录不存在、而重命名目录存在**时，
把对应常量改指后者；源工程内经典目录都在 ⇒ 该函数**不触发**，行为与原状逐字一致
（已实测：源工程常量仍解析为 `01_data/dataset`、`03_weights` …）。

### 并新增 `ensure_dataset_yaml()`：`dataset/wall_defects.yaml` 的 `path` 是训练机
绝对路径（作者为规避「ultralytics 解析相对路径在中文目录下会失败」而刻意写绝对路径，
见 `write_dataset_yaml` docstring 的实测说明），拷到别的机器即失效。
`ensure_dataset_yaml()` 在 `evaluate.py` / `train.py` 启动时探测：若 `path` 指向的数据不存在、
而包内数据存在，就**就地改写**为当前位置（幂等）。这样**保留绝对路径的设计意图**，
同时让包可移植。

**E. 修复后实机验证（在交付包内真跑，非静态检查）**

| 验证项 | 结果 |
|---|---|
| `evaluate.py --weights=../weights/v11s640_best.pt` | ✅ 跑通，**mAP50=0.7230 / mAP50-95=0.4410 / P=0.7707 / R=0.6693** |
| 与源侧权威 `v11s640_test_metrics.json` 比对 | ✅ `overall` 与 `per_class` **逐值相同**（仅 `weights` 字段记的是相对路径，更可移植） |
| `evaluate.py --all`（全权重） | ✅ 跑通，v11s640 0.7230、v11s640_clsbal 0.7281 —— 与 §7.8.6 逐种子表一致 |
| `pipeline.py`（单图端到端：检测→GSD→几何→分级） | ✅ 跑通，正确输出「GSD=0.5mm/px ⇒ 0.3mm 裂缝判读=insufficient ⇒ 拒绝给毫米结论」 |
| 布局探测后所有常量 | ✅ `dataset / weights / results / grading_rules / deploy / report` 全部 exists=True |
| 源工程行为 | ✅ 常量仍为 `01_data/dataset / 03_weights / …`，**未受影响** |

**F. 顺带加固 `logs/_pack_vs_src.py`（一致性核查脚本）**
原脚本把这 2 类**本该存在**的差异误报成 FAIL，本次新增「**已识别·有机差异**」类别：
① `dataset yaml` 的 `path` 指向各自机器（其余字段一致）；
② 仅行尾不同（归一 `\r\n`→`\n` 后字节级相同）。
另外把 README 比对升级为「载体路径写法归一后比对」（源 README 写源路径、包 README 写包内路径，
两者**本就应不同**）；并把 ultralytics 训练副产物 `*.cache` 列入排除（跑一次评估就会重新生成，
不该进包）。
**终态：真实内容不同 = 0 / 包有源无 = 0 / 需复核 = 0 = PASS**，另 2 项为已识别有机差异。

**G. 为什么此前 4 轮审查都没抓到**
历次审查**都在源工程里跑脚本**（源路径齐全 ⇒ 永远成功），从未**在交付包内**跑过。
这正是 §1.6 纪律「静态检查不够、必须真跑目标函数」的又一次应验 ——
**而「真跑」还必须是「在交付载体上真跑」，不能只在源上真跑**。
> **新增纪律**：凡「交付包」类产物，验证必须在**包自身目录内**执行入口命令至少一次，
> 且核对输出数字与报告一致。

_本报告由 2026-09-24 的赛题符合性复核生成。_
_上一版诊断见 `审查报告_赛题符合性大检查_20260922.md`（仅诊断）。_
