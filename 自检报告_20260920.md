# 外墙缺陷智能筛查 —— 全项目自检报告

- 生成时间：2026-09-20 17:1x（UTC+8，以机器时钟为准）
- 自检范围：`D:\pythonstudy 备份\创新题\外墙缺陷筛查\` 全量 14,384 个文件 / 1.6 GB
- 自检方法：文件树全量盘点 + 22 个 `.py` 静态语法编译 + 训练/评测/量化三类产物逐个解析 + 四份报告文档与产物交叉核对 + 前端 DOM/文案与实现对照
- 口径原则：**只认产物文件里的实测值**；报告里的数字只要能追溯到文件就保留，追溯不到的一律标为「不可复核」

> 本报告只做「盘点 + 取证 + 提出问题」，**不擅自修改任何被证据推翻的内容**。
> 所有需要改写的点已在第五节列出，等您拍板。

---

## 一、一句话结论

工程主体（数据管线、训练、评测、量化、分级、部署、收集站）**都已跑通并留下产物**；
但**发现 2 处 P0 级事实性错误**（`v8n640` 用错数据集训练、`KeyError` 归因错误）、
**6 类 P1 级口径/事实问题**、以及 10 余处 P2 级清理项。
其中 P0 会直接影响 §5.3 / §7.1 / 答辩材料 / 演示脚本的论证成立性。

---

## 二、已完成（可交付 / 已验证）

### 2.1 代码管线（22 个模块，语法 22/22 通过）

| 类别 | 模块 | 状态 |
|---|---|---|
| 数据下载 | `step0_download.py`、`step0b_download_modelscope.py` | ✅ 已产出 `01_data/raw` |
| 扫描/去重 | `step1_scan.py`、`step2_dedup.py` | ✅ 已产出 `01_data/dedup` |
| 统一/划分 | `step3_unify.py`、`step4_split.py` | ✅ 已产出 `01_data/unified`、`01_data/dataset` |
| 露筋补充 | `step5_fetch_rebar.py` | ✅ |
| 训练 | `train.py` | ✅ 三个 run 已产出权重 |
| 量化 | `step6_quantize.py` | ✅ 本轮修复 5 处后重跑成功 |
| 评测 | `evaluate.py`、`run_experiments.py`、`exp_ablation.py`、`exp_domain_shift.py`、`exp_robustness.py` | ✅ |
| 业务算法 | `common.py`、`gsd.py`、`measure.py`、`grade.py`、`rectify.py`、`pipeline.py` | ✅ |
| 工具 | `_extract_grading_rules.py`、`_verify_rectify.py` | ✅ |

- 证据文件：`logs/_review_pycompile.txt`（`exit=0`）、`logs/_audit_full.txt` 第 4 节「语法通过 22 / 失败 0」

### 2.2 数据集

- 正式集 `01_data/dataset/wall_defects.yaml`：**7 类**（crack / spalling / efflorescence / exposed_rebar / rust / delamination / moss）
- 划分实测：**train 455 / val 129 / test 65**（images 与 labels 一一对应）
- 证据：`logs/_audit_smoke.txt` 第 204-207 行

### 2.3 训练与权重（实测量，字节级核对）

| 权重 | 字节数 | 折算 |
|---|---|---|
| `v8s640_best.pt` | 22,550,449 B | 21.51 MiB |
| `v11s640_best.pt` | 19,214,673 B | 18.32 MiB |
| `v8n640_best.pt` | 6,252,529 B | 5.96 MiB |

- v8s640：100 epoch（`results.csv` 末行 epoch=100）；v11s640：84 epoch（续训后）
- 证据：`logs/_audit_full.txt` 第 3 节、`logs/_audit_train.txt` 第 41-64 行

### 2.4 评测指标（**唯一可引用口径 = 独立测试集**）

| run | P | R | mAP50 | mAP50-95 | 体积 |
|---|---|---|---|---|---|
| `v8s640` | 0.67888 | 0.65928 | **0.69798** | 0.39175 | 21.51 MiB |
| `v11s640` | 0.60275 | 0.71639 | 0.69243 | **0.39692** | 18.32 MiB |
| `v8n640` | 0.00536 | 0.14851 | 0.00249 ⚠️ 无效 | 0.00075 | 5.96 MiB |

- 证据：`04_results/eval/v8s640_test_metrics.json`、`v11s640_test_metrics.json`、`v8n640_test_metrics.json`
- **重要局限（已知并如实记录）**：test 集中 `exposed_rebar` / `rust` / `delamination` **三类各 0 实例**，7 类模型实际只有 4 类可评。

### 2.5 量化（本轮重跑，取得唯一口径）

| run | 校准法 | 实际校准图 | INT8 体积 | 压缩比(vs FP32 ONNX) | 状态 |
|---|---|---|---|---|---|
| `v8n640` | Entropy | **96** | 3.29 MB | 3.56× | `int8_unusable` |
| `v8s640` | Entropy | **64** | 11.11 MB | 3.84× | `int8_unusable` |

- 端到端检出（真图 10 张，conf=0.001）：`v8n640` FP32 1567 → INT8 **0**；`v8s640` FP32 506 → INT8 **0**
- INT8 CPU/ORT 延迟 p50：`v8n` 48.78 ms / `v8s` 117.2 ms（硬件口径：CPU + CPUExecutionProvider）
- FP32 GPU 延迟 p50：`v8n` 5.31 ms / `v8s` 4.75 ms（硬件口径：CUDA）
- 证据：`04_results/quant/quant_report.json`（6,413 B，17:11:14 写入）、`logs/_audit_quant_final.txt`

### 2.6 环境修复（本轮核心成果，已三重验证）

- 根因定案：`ultralytics` 在首次 `predict/val` 构造 `AutoBackend` 时（`nn/backends/onnx.py:63,76`）判定 device 为 CUDA → 触发 `checks.py:595` 的 `@Retry` → 自动 `pip install onnxruntime-gpu` → 覆盖本机 CPU 版 ORT，导致整条推理链报废。
- 修复手段：`ULTRALYTICS_SKIP_REQUIREMENTS_CHECKS=1`（在函数内求值，与导入顺序无关，最可靠）+ `device="cpu"` 显式传入。
- 验证方式：三组对照实验 + `[5b]` 复跑。本轮重跑全程打印 4+ 次 `skipping requirements check`，**零 pip 进程**，ORT 1.29.0 全程健康。
- 代码修复：`02_code/step6_quantize.py` 共 5 处（模块顶部开关、docstring 危险建议删除、val 调用注释、`verify_int8_detects` 逐行源码注释、`YOLO_OFFLINE` 误用纠正）。
- 证据：`logs/_audit_quant_final.txt`、`logs/_probe_autoinstall_guard*.txt`

### 2.7 业务模块与部署

- `05_quantify_grade/`：**已完成**（`grading_rules.json` 4,334 B，由 `_extract_grading_rules.py` 从 `gsd.py`/`grade.py` 导出，附 `_extract_report.txt` 留痕），含 GB 50010 裂缝限值与 JGJ 125 危险点。
- `06_deploy/app.py`：语法通过、Gradio 分流正确、依赖调用名与权重路径全部真实、`grading_rules.json` 无漂移。
- `08_photo_collector/site/`：DOM id 双向差集为空、`config.js` 无明文密钥、存储路径与文档一致、采集了拍摄距离。

### 2.8 文档与记忆

- `07_report/技术报告.md`：§8.2 已重写为四段式进展（根因链条 / 两道开关 / `[5b]` 复现 / 重跑已启动）；§8.3 标题纠正为「五个工程坑」并新增坑 5（自动安装毁环境）。
- `logs/_quant_report_PRESERVED_1244round.json`：旧报告哈希备份完好。

---

## 三、发现的问题清单

### 🔴 P0-1 · `v8n640` 训练用错数据集（最重大）

- **位置**：`04_results/train/v8n640/args.yaml`
- **证据**：
  ```
  data    = D:\pythonstudy 备份\.workbuddy\reference\_smoke\dataset\wall_defects.yaml
  epochs  = 2
  ```
  `_smoke` 数据集实测：`names` 只到 5（**6 类，缺 moss**）；图片 **train 6 / val 3 / test 2 = 共 11 张**。
  `results.csv` 末行：`2, ..., metrics/mAP50(B)=0.02369`。
  `03_weights/v8n640_best.pt` 与 `04_results/train/v8n640/weights/best.pt` 字节数一致（6,252,529 B）。
- **推论**：报到报告里的 `v8n640` mAP50 0.0025 **不是「基座容量不足」，而是「用 11 张图的冒烟集训了 2 个 epoch」**。
  量化日志中 `best.pt` 输出 shape 为 `(1, 10, 8400)`（= 4 + **6** 类），而 `v8s640` 为 `(1, 11, 8400)`（= 4 + **7** 类）—— 这是在产物层面留下的铁证。
- **影响面（约 30 处）**：
  - `技术报告.md:480`（§5.3「成因是容量不足」）
  - `技术报告.md:706`（§7.1 表格「容量不足，见 §5.3」）
  - `技术报告.md:1149`（「v8n 输在基座容量不足」）
  - `README.md:40`（「不可用 —— 容量不足的负结果」）
  - `答辩材料.md:83`（「❌ 容量不足，完全不可用」）
  - `演示视频脚本.md:159`、`:267`（口播 0.0025「不可用」）
- **建议**：`v8n640` 这一格要么重训（正式集 7 类 / 100 epoch），要么整体降级为「冒烟验证用，不构成规格结论」。**在决定前，报告里凡引用 0.0025 的论证都处于待定状态。**

### 🔴 P0-2 · `KeyError: 3` 归因错误

- **位置**：`技术报告.md:713-719`
- **现状表述**：「`ultralytics` 绘图阶段越界（`KeyError: 3`）……原因是 `ap_per_class` → `plot_pr_curve` 用类别 id 索引 `names` 时越界，通常代表该模型有类别完全没检出」。
- **真实原因**：**模型 6 类 vs 数据集 7 类发生类别错配**（与 P0-1 同源）。索引 3 在模型的 `names`（只有 6 项，0~5）上合法、在数据集（7 项）上才对不上——本质是打标/评估的类别空间不一致，不是绘图缺陷。
- **建议**：与 P0-1 一并改写。

### 🟠 P1-1 · 校准集数字四处打架

| 出处 | 说法 |
|---|---|
| `技术报告.md:997` | 校准集从 val 取 **150 张** |
| `技术报告.md:1013` | 实际使用 **32 张**（150 张时 OOM） |
| `技术报告.md:1032` | 请求 / 实际 = 150 / **129 张** |
| `quant_report.json`（新） | `calib_n: 150`（**请求值**）/ `calib_used` = v8n **96** / v8s **64** |
| 量化日志 | 129 → 128 → 96 → 64 逐级 OOM 重试，v8n 在 96 成功、v8s 在 64 成功 |

- **隐患**：`calib_n` 字段名会被误读为实际值，实为请求值。
- **建议**：统一口径为「请求 150 张，实际 v8n 96 / v8s 64（129/128/96 均 OOM）」；并把 `calib_n` 改名为 `calib_n_requested`（或加显式注释）。

### 🟠 P1-2 · 「v11s 体积是 v8s 的 3.4 倍」已被证伪（跨轮次混用）

- **位置**：`技术报告.md:454`、`:513`、`:708`、`:1519-1520`；`答辩材料.md:85`、`:94`；`README.md:39`；`演示视频脚本.md:266`
- **证据**：报告引用的 `72.50 MB / 0.7497 / 0.4133` 来自 `logs/eval_final.log` 的 **12:54 旧一轮评估**；而 16:35 续训完成后的 `v11s640_best.pt` 实测为 **18.32 MiB**，`train_summary.json` 记录的 val mAP50 为 0.7541。
- **后果**：`18.32 MiB < 21.51 MiB` —— v11s **体积反而更小**，「3.4 倍」是跨轮次混用造成的假象。
- **建议**：改为「v11s 体积更小（18.32 MiB vs 21.51 MiB）」。

### 🟠 P1-3 · §5.3 表格把 v8s 的 test 指标与 v11s 的 val 指标并列比较

- **位置**：`技术报告.md:454-455`
  ```
  | mAP50    | 0.6980 | 0.7497 | v11 高 +0.0517 |
  | mAP50-95 | 0.3917 | 0.4133 | v11 高 +0.0216 |
  ```
  左侧 0.6980 / 0.3917 = `v8s640` 的 **test** 指标；右侧 0.7497 / 0.4133 = `v11s640` 的 **val** 指标。
- **同一口径下的真实对比（test vs test）**：mAP50 `v11s 0.69243 < v8s 0.69798`（v8s 更高）；mAP50-95 `v11s 0.39692 > v8s 0.39175`（v11s 略高）—— **互有胜负**，不是「四类全胜」。
- **建议**：统一到 test 口径重写该表与结论。

### 🟠 P1-4 · INT8 延迟数字未同步

| 出处 | 旧值 | 新报告值 |
|---|---|---|
| `技术报告.md:1014` / `:1180` | `v8n` **39.19 ms** | **48.78 ms** |
| `技术报告.md:1014` / `:1033` | `v8s` **104.29 / 129.47 ms** | **117.2 ms** |

- **建议**：按 `quant_report.json` 回填，并注明硬件（CPU / CPUExecutionProvider）。

### 🟠 P1-5 · 前端三处文案与实现矛盾（`08_photo_collector`）

| # | 位置 | 文案 | 实现事实 |
|---|---|---|---|
| 1 | `site/index.html:556` | `<b>0</b> 个人隐私收集` | `:573`/`:577` 强制邮箱 + 密码注册 |
| 2 | `site/index.html:697` | 「不用注册也能先看看大家拍了什么。」 | `#stat-count`(`:688`) 与 `#recent`(`:689`) 位于 `:619` 的 `<section id="view-main" hidden>` 内 → **未登录恒不可见** |
| 3 | `推广文案.md:207` | 「用邮箱验证码登录就行，不用给密码」 | `site/app.js:103-127` 存在密码登录/带密码注册路径 |

- **建议**：改写文案，或调整 DOM 让统计区块在未登录时可见。**对外发布前必须处理 #1/#2，否则属于不实陈述。**

### 🟠 P1-6 · JGJ 125 危险判据在 Web 演示中永不触发

- **位置**：`02_code/grade.py:69-91`
- `grade_crack(..., is_main_rebar_zone, is_slab_tension)` 两个开关**从未被 `06_deploy/app.py` 传入过**（子代理核查结论）。
- **后果**：0.5 mm（主筋处）/ 1.0 mm（板受拉）两级危险判据在演示里无法展示，只有 GB 50010 的 0.30/0.20 mm 生效。
- **建议**：要么在 UI 暴露这两个选项，要么在报告/答辩中明确说明「演示路径只走到 GB 50010」。

### 🟡 P2 级（不影响结论，但属缺陷/冗余）

1. `02_code/grade.py:112` 用魔数 `8` 判可测量下限，应为常量 `COMFORTABLE_PX`（= 8.0）——语义一致性。
2. `06_deploy/app.py:442-443` 注释算错；`app.py:178-179` 为死代码。
3. `04_results/eval` 冗余：
   - `v8s640_test_test_metrics.json`（2,141 B）—— 指标与 `v8s640_test_metrics.json` **逐字相同**，但 `weights_mb` 写 **85.39**（未剥离的原始 best.pt），文件名 `_test_test` 重复。
   - `_archive_smoke_20260920/`（0 字节目录）
   - `_pipeline_smoke2.json`（692,811 B，冒烟产物）
4. `02_code` 目录污染：`runs/` **138 文件 76.1 MB**（含大量空 `val-*` 目录）、`weights/` 1 文件 5.3 MB、`_verify_out/` 37 文件 36.9 MB、`__pycache__/` 22 文件 450.5 KB。
5. 全树 **58 个空目录**（多在 `01_data/raw/_archived_*` 与 `02_code/runs/detect/val-*`）。
6. 「85.39 MB / 72.50 MB」是**未剥离的原始训练 checkpoint**口径，与「21.51 MiB / 18.32 MiB」的成品口径混用 —— 报告里必须二选一并标注。
7. 记忆文件曾把 `05_quantify_grade/` 标注为「暂空」，实际该目录**已完整**（3 文件 11.7 KB）—— 记忆条目已过时。
8. ~~`_grun.txt`（279 B）与 **PID 29448** 的 Gradio 演示服务仍在占用 7861 端口。~~
   → ✅ **已处置（2026-09-20 18:37 用户批准）**：PID 29448 已关闭，7861 端口已释放
   （实测 `netstat` 无监听）。`_grun.txt` 句柄应已随之释放，可由后续清理流程处理。
9. `test_eval_summary.json` 中 test 集每类 `instances: null`，无法从产物直接核对每类实例数。
   → ✅ **已修复（2026-09-20 17:59）**：根因是 ultralytics `Metric` 类**没有 `np_per_class`**
   （旧代码 `getattr` 恒得 `None` 且**不报错**）；已改用 `nt_per_class`/`nt_per_image`
   并按**类名对齐**。验证脚本 `logs/_verify_eval_instances.py` **PASS**，
   合计 **259** 与划分报告一致（两条独立路径交叉验证）。

---

## 四、尚未完成

| # | 事项 | 阻塞原因 |
|---|---|---|
| 1 | `v8s1024` 训练（配方已定义在 `train_summary.json` recipes） | 从未实际产出；长任务 |
| 2 | 演示视频 | 需您录屏 |
| 3 | 答辩 PPT | 您明确「先不做」 |
| 4 | 补充自拍照片 | 等网友投稿 |
| 5 | 最终工程包 → `D:\pythonstudy 备份\种子杯项目报告` | 按约定，仅全部完成后拷贝 |
| 6 | 残留清理（`_grun.txt` / `02_code/runs` / 空目录 / `logs/_trash_here` / `logs/_famerL_tmp`） | ⚠️ **仍未执行**。2026-09-21 实测清单：空目录 **96 个**（初版记 58 个系 V1 快照）、`logs/_trash_here` **45 文件 / 4.6 GB**、`logs/_famerL_tmp` **2 文件 / 1.7 GB**、`02_code/runs/detect/val-*` **41 个空目录**。清理属破坏性操作，**待用户确认后执行**（见 §7.3 P2-5） |
| 7 | 用新 test 指标回填四份报告 | ✅ **已完成（V2 已定稿，见 §7.0）** |

---

## 五、需要您拍板（我未擅自改动）

1. **`v8n640` 是否重训**（正式集 7 类 / 100 epoch）？还是把它降级为「冒烟验证」，从规格对照里删除？
2. 报告里**约 30 处**引用 `v8n640` 0.0025 的论证（含「容量不足」归因）是否按新证据整体改写？
3. `KeyError: 3` 的归因（`技术报告.md:713-719`）是否改写为「类别错配」？
4. 校准集口径统一成哪个版本？（建议：请求 150 / 实际 v8n 96、v8s 64）
5. `v11s` 体积与精度两处反转（§5.3 表格、§8.x 结论）如何表述？
6. 前端三处文案是否改？（尤其 `index.html:556` 的「0 个人隐私收集」与 `:697` 的「不用注册也能先看看」）
7. 露筋源里 **79 个 `crack` 框（25 图）** 是否并入主数据集？——并入会改变**全部已报指标**。
8. ~~照片收集站存储是否改为私有桶？（当前 `shared/<uid>/facade/`，所有登录用户可读）~~
   → ✅ **已处置（2026-09-20 18:39，用户批准）**。
   但**我最初的说法是错的，须如实记录**：「改成私有桶」**这个操作不存在** ——
   官方 SDK 只有唯一 logical bucket `runtime`，**不支持切换桶可见性**，也不支持 public bucket/URL
   （`cloud-service/references/storage/code-generation.md:197-202`）。
   真正可选的只有两种 scope（`management.md:44-48`）：
   | Scope | 前缀 | 谁能读 |
   |---|---|---|
   | `user_private` | `users/<uid>/...` | 上传者本人 + 管理员 |
   | `app_shared` | `shared/<uid>/...` | **所有已登录用户** + 管理员 |

   已据此把 `cloud.js` 上传路径 `sharedPath` → **`userPath`**（`_patch_storage_private.py`，2/2 命中）。
   **实测改前状态：`photo_submissions` count = 0、storage 无任何对象** ⇒ 无历史遗留，切换干净。
   同步把对外文案从「不公开邮箱」加强为「**不对其他网友公开**」（6 处，见下方 §7.4），
   并**修正了边界**：不能说「除了我谁也看不到」——研究者本人确实需要能读照片。
9. `rectify()` 是否接入 `pipeline.py`？——会改变上报的毫米数字。
   → ✅ **已定案并接入**（`--rectify`，验证 18/18，见技术报告 §6.5.1）。
10. ~~PID 29448 的 Gradio 演示服务是否关掉？~~
   → ✅ **已关闭（2026-09-20 18:37，用户批准）**，7861 端口已释放。

> 已定论、无需拍板：**绝不安装 `onnxruntime-gpu`**（与 CPU 版共用 `onnxruntime/` 包目录，会覆盖本机 ORT）。

---

## 六、证据文件索引

| 文件 | 内容 |
|---|---|
| `logs/_audit_full.txt` | 9 节主审计（环境/文件树/交付目录/权重量/语法/污染/报告/结果结构/量化/记忆） |
| `logs/_audit_eval.txt` | eval 目录 9 个 JSON + 5 个 CSV 逐份解析 |
| `logs/_audit_train.txt` | 三个 run 的 `args.yaml` + `results.csv` 末行 + 权重对照 |
| `logs/_audit_smoke.txt` | `_smoke` 数据集全清单 + 两个 `wall_defects.yaml` 对照 + 正式集划分统计 |
| `logs/_audit_quant_final.txt` | 量化终态 `quant_report.json` 全文 + 滤条日志 |
| `logs/_review_pycompile.txt` | 22 模块语法编译结果（`exit=0`） |
| `logs/quant_rerun_20260920c.log` + `.exit` | 本轮量化重跑完整日志（exit 0） |
| `logs/_quant_e2e_5b.json` | `[5b]` 端到端体检结果 |
| `logs/_probe_autoinstall_guard*.txt` | 三组对照实验证据 |
| `logs/eval_final.log:89,101` | 报告 `72.50 MB / 0.7497 / 0.4133` 的真实出处（12:54 旧轮次） |
| `logs/train_v11s640_resume.log:2438-2440` | v11s640 续训后新数字（18.32MB / val mAP50 0.7541） |

---

## 七、处置记录（2026-09-20 追加 · 本节由后续修补回填）

> 上面的「问题清单」保持**原样不动**，作为当时快照；本节只记录每一项的处置结果。
> 每一条都注明**依据的文件**，可自行复核。

### 7.0 V2 定稿状态（2026-09-21 追加 · 阅读本报告前请先看这一节）

> ⚠️ **本报告第二、三节的实测数字是 2026-09-20 17:1x 的 V1 快照。**
> 其中 §2.2（`455 / 129 / 65`）、§2.4（评测指标表）、§四 #1（`v8s1024` 未产出）
> **均已被 V2 取代**。问题清单按原样保留（作为当时快照），**但数字不要引用**。
> **对外一律以 `07_report/README.md`「核心数字速查」+ `技术报告.md` §4/§7 的 V2 口径为准。**

**V2 定稿的关键差异（V1 → V2）**：

| 项 | V1 快照（本报告 §2） | **V2 定稿（现行口径）** |
|---|---|---|
| 数据集划分 | 455 / 129 / 65 | **473 / 134 / 67**（674 对；实例 1734 / 501 / 259） |
| 并入内容 | — | 露筋源 **79 个 `crack` 框 / 25 图** 已并入主集 |
| 主力模型 | `v8s640`（0.6980） | **`v11s640`**（test mAP50 **0.7628** / mAP50-95 **0.4407** / 18.33 MB） |
| `v8s640` | 0.69798 | 0.6766 |
| `v8s1024` | 未产出 | **0.6596**（训练 1024 / 评估 640，该行被低估） |
| `v8n640` | 无效 0.00249 | **0.6580**（已按正式集 7 类 / 100 epoch 重训） |
| 报告回填 | 进行中 | ✅ **四份材料已全部回填 V2 口径** |

> **口径纪律**：对外精度**只能**取自独立测试集（`split=test`）；
> 模型间 mAP50 差异 **> ≈0.03** 才可认为真实（见 `技术报告.md` §7.1.1）。

### 7.1 已修复（代码）

| 编号 | 问题 | 处置 | 依据 |
|---|---|---|---|
| **P0-1** | `v8n640` 训练用错数据集 | ✅ **已重训 + 四份材料归因已改正**。V2 队列按正式集（7 类 / 100 epoch）重训；四份材料里「容量不足」的归因已改为「冒烟配置」，数值标注为**无有效指标 / 待回填** | `logs/_patch_v8n_attrib.txt`（18/18 命中） |
| **P1-5 备选方案** | 照片站存储「是否改私有桶」（用户 2026-09-20 18:37 批准按建议执行） | ✅ **已处置，但我的原始说法被证伪**：**「改成私有桶」这个操作不存在** —— SDK 只有唯一 logical bucket `runtime`，**不支持切换桶可见性**，也不支持 public bucket/URL。真正可选的只有两种 scope。已把上传路径由 `sharedPath` 改为 **`userPath`**（`users/<uid>/facade/`）：可见性＝**上传者本人 + 管理员**，其他登录网友读不到。**改前实测 `photo_submissions` count = 0、storage 无任何对象** ⇒ 零迁移成本。同步加强对外文案 6 处（详见 **§7.4**） | `08_photo_collector/_patch_storage_private.txt`（2/2 命中）；`logs/_STORAGE_SCOPE_FINDING.md`；`logs/_final_storage_verify.txt` |
| **P0-2** | `KeyError: 3` 归因错误 | ✅ **已改写为「类别错配」** | `evaluate.py` + 技术报告 §7.1 |
| **P1-1** | `calib_n` 字段被误读为实际值 | ✅ **改名 `calib_n_requested`**，并新增 `calib_n_note` 指向 `calib_used` | `logs/_patch_jgj125_calib.txt` D1a/D1b |
| **P1-2** | 「v11s 体积是 3.4 倍」被证伪 | ✅ **四份材料已改**。并查明**更精确的根因**：72.50 MiB 是 `v11s640_best_ep71.pt`（**未剥离优化器**的中途 checkpoint），21.51 MiB 是剥离后的成品 —— **不只是「跨轮次」，而是「跨产物形态」**。成品口径下 v11s **18.32 < v8s 21.51 MiB** | `logs/_patch_v8n_attrib.txt` T5/V1/W2/X2；`logs/_weights_all.txt` |
| **P1-3** | §5.3 把 v8s 的 test 与 v11s 的 val 并列 | ✅ **已统一到 test 口径**并用 `test_per_class.csv` 重算每类。真实结论：**2 : 2 互有胜负**（v8s 赢 crack +0.0711、spalling +0.0748；v11s 赢 efflorescence +0.0713、moss +0.0525） | `_archive_.../eval/v8s640_test_per_class.csv`、`v11s640_per_class.csv`（均 `split=test`） |
| **P1-4** | INT8 延迟未同步 | ✅ **已回填**：`v8n` **48.78 ms** / `v8s` **117.2 ms**（CPU + `CPUExecutionProvider`） | 17:11 轮 `quant_report.json` |
| **P1-5** | 照片站三处文案与实现矛盾 | ✅ **已改** | `index.html` / `推广文案.md` |
| **P1-6** | JGJ 125 危险判据永不触发 | ✅ **已接通**：`grade_all` 透传 + app 勾选组 + `--jgj125`；`logs/_verify_jgj125.py` **27/27**；默认路径零回归 | `logs/_verify_jgj125.txt`；报告 §6.3.1 |
| **P2-1** | `grade.py` 魔数 `8` | ✅ **改为引用 `gsd.COMFORTABLE_PX`** | 同上 A6/A7 |
| **P2-4** | `02_code/runs` 污染 76 MB | ✅ **已归档**至 `_archive_20260920/housekeeping_20260920/`；另删 2 个空目录 | `logs/_cleanup_result.txt` |
| **P2-6** | 85.39 / 72.50 MB 与成品口径混用 | ✅ **已给出二者的逐一定位表**并统一到成品口径 | 报告 §8.2 |
| **P2-9** | `test_eval_summary.json` 每类 `instances: null` | ✅ **已修复**。根因：ultralytics 8.4.138 的 `Metric` 类**没有 `np_per_class` 属性**（旧代码 `getattr(metrics.box, "np_per_class", None)` 恒为 `None`）；真正可用的是 `DetMetrics.nt_per_class` / `nt_per_image`（`metrics.py:1174` 由 `stats["target_cls"]` 统计）。改为**按类名对齐**（避开 id 顺序错配），并顺带新增每类 `images` 字段。真实解码验证：crack 58 / spalling 30 / efflorescence 118 / moss 53 = **259**，与 V2 测试集实例数完全一致 | `logs/_patch_eval_instances.txt`（6/6 命中）、`logs/_verify_eval_instances.txt`（PASS） |

### 7.2 已定案（口径）

| 事项 | 定案 |
|---|---|
| 量化过程数字打架 | ✅ **以 17:11 轮为准**（Entropy / 实际 96 与 64 张 / 48.78 与 117.2 ms）。12:44 轮（MinMax / 129 张 / 129.47 ms）报告**原样保留**在 `logs/_quant_report_PRESERVED_1244round.json`，未改动 |

### 7.3 仍未处置（需用户拍板或依赖新指标）

| 编号 | 事项 | 状态 |
|---|---|---|
| ~~P1-5 备选方案~~ | ~~照片站存储是否改私有桶~~ | ✅ **已处置（见 §7.1）** —— 并已更正为「换 scope」的准确表述 |
| P2-2 | `app.py:442-443` 注释算错 / `178-179` 死代码 | ✅ **已定案（2026-09-21 逐行复核）**：① 逐行阅读确认，**当前版本这两处行号指向的代码都没有所述问题** —— `442-443` 现为 `_default()` 的 `return o.tolist()` / `raise TypeError(...)`（是**完整的 JSON 兜底序列化函数**，负责把 `numpy.bool_`/`np.integer`/`np.floating`/`ndarray` 转成原生类型，**不存在「注释算错」**）；`178-179` 现为 `cv2.cvtColor(image_rgb, cv2.COLOR_RGB2BGR)` / `h, w = img.shape[:2]`，`h, w` 后续在 `:227`/`:353` 确有使用，**不是死代码**。⇒ 结论：所述问题源自**更早的修订版**，行号已随时间失效；**当前版本无需改动，该项结案**。② 但排查中发现**本轮新引入的一句注释是错的**（`app.py` 分级段：原写「勾选后 0.6mm 裂缝会从 attention 升为 danger」，与实测不符 —— 二类环境 danger 门槛为 `2×0.20=0.40mm`，0.60mm 本来就是 danger），**已改正**并补上「只有一类环境且宽度落在 `(0.50, 0.60] mm` 才会翻转等级」的实测结论；`py_compile` 通过、`_verify_jgj125.py` 重跑 **27/27** |
| P2-3 | `04_results/eval` 冗余项 | ✅ **已核实（2026-09-21）**：目录里仍有 `v8s640_final_test_metrics.json`（2,274 B）/ `v8s640_final_per_class.csv` 与 `v8s640_test_metrics.json`（2,251 B）/ `v8s640_per_class.csv` **两套并存**，二者**指标逐字相同**（`mAP50 0.67662` / `mAP50-95 0.3945` / 逐类 AP 全等），唯一差别是 `run` 名与 `weights` 指向：`_final` 版指向 `04_results/train/v8s640/weights/best.pt`，非 `_final` 版指向 `03_weights/v8s640_best.pt` —— **同一模型的两种落点**。四份报告引用的都是**非 `_final` 版**，故 `_final_*` 属冗余，可归档（不改变任何已报数字） |
| P2-5 | 全树空目录 | ⚠️ **仍在，2026-09-21 实测 96 个**（本报告初版记 58 个，系 V1 快照）。分布：`02_code/runs/detect/val-*` **41 个**、`_archive_20260920/housekeeping_20260920/runs_ultralytics_val_plots/` 约 **40 个**、`01_data/raw/_archived_gitempty_20260920/` **8 个**、`01_data/raw/greybrick/src/{train,valid,label}/{images,labels}` **5 个**。**均为空壳（0 文件），删除无数据风险；但属破坏性操作，待用户确认** |
| P2-8 | `_grun.txt` + **PID 29448**（占 7861 端口） | ✅ **已处置（用户 2026-09-20 18:37 批准关闭）**：PID 29448 已关闭，`netstat` 实测 7861 端口**已无监听**。`_grun.txt` 句柄随之释放，可交由后续清理流程处理 |
| P2-9 | ~~`test_eval_summary.json` 每类 `instances: null`~~ | ✅ **已移入 §7.1**（已修复） |
| 四、#7 | 用新 test 指标回填四份报告 | ✅ **已完成（V2 已定稿）**：四份材料已按 **473/134/67** 与四档 test 指标回填；主力定为 **`v11s640`**（0.7628）。见 §7.0 |

### 7.4 照片站存储改动明细（2026-09-20 18:39）

**代码**（`08_photo_collector/site/cloud.js`）：

| 项 | 改前 | 改后 |
|---|---|---|
| 上传路径 | `cloud.storage.sharedPath(uid, 'facade/…')` | **`cloud.storage.userPath(uid, 'facade/…')`** |
| 可见性 | **所有已登录用户** + 管理员 | 上传者本人 + 管理员 |

**对外文案**（6 处，全部由「不公开邮箱」加强为「不对其他网友公开」）：

| 文件 | 位置 |
|---|---|
| `site/index.html` | 提交区注意事项 |
| `推广文案.md` | §2 风险节（整节改写）／欢迎语／FAQ／发布前检查清单 |
| `发布话术.md` | 话术正文／FAQ／发布前检查清单 |

**⚠️ 修正了一个措辞边界**：不能说「除了我谁也看不到」或「绝不外流」——
**研究者本人（项目作者）为做算法训练必须能读照片**，这是设计使然。
所以精确表述是「**不会对其他网友公开**」，而不是笼统的「绝对安全」。
两份文案原有的「不要承诺照片绝对不会外流」纪律**保留**，只是把可承诺的范围上调了。

**证据**（注：这 4 个留痕文件位于 `08_photo_collector/`，不在 `logs/`）：
`08_photo_collector/_patch_storage_private.txt`、`08_photo_collector/_patch_site_copy.txt`、
`08_photo_collector/_patch_promo_storage.txt`、`08_photo_collector/_patch_promo_storage2.txt`、
`logs/_final_storage_verify.txt`（旧表述残留检查 = **0**）。
