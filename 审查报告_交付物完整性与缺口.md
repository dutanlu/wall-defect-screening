# 外墙缺陷筛查项目 —— 全量只读审查报告（交付物完整性 & 缺口）

> **审查性质：严格只读。** 除本报告为新建文件外，**未修改、未移动、未删除任何现有文件**。
> **审查基准**：《YOLO 视觉应用创新赛》赛题要求 —— `.workbuddy/reference/YOLO视觉应用创新赛_赛题_全文.txt`（5,881 B）
> **审查方法**：文件树全量盘点 + 关键文档逐份精读 + 报告与产物交叉核对
> **本次新增的只读工具**（可复核，均为只读脚本）：
> `logs/_audit_more.py`、`logs/_audit_more2.py`、`logs/_audit_datasrc.py`
> 及输出 `logs/_AUDIT_MORE.txt`、`logs/_AUDIT_MORE2.txt`、`logs/_AUDIT_DATASRC.txt`
> **生成时间**：以机器时钟为准

---

## 一、结论速览

**工程主体已经完整、自洽、可交付。** 但存在 **4 项 P0 级缺口**（其中 1 项是赛题明文"必须交付"、另 1 项是答辩必被追问的数据问题），以及若干 P1/P2 一致性与清理项。

| 赛题「必须交付」 | 状态 | 载体 |
|---|---|---|
| ① 可运行的检测系统（demo 或完整应用） | ✅ 已具备 | `06_deploy/app.py`（1,022 行 Gradio）+ `02_code/pipeline.py` + `08_photo_collector/site/` |
| ② 项目代码与部署文档 | ✅ 已具备 | `02_code/`（23 个 `.py`）+ `06_deploy/README.md` + 技术报告 §10 |
| ③ 技术报告（问题分析 / 方案设计 / 实验结果） | ✅ 已具备 | `07_report/技术报告.md`（123.4 KB，10 章 + 附录 A/B） |
| ④ **演示视频或实机展示** | ❌ **缺失** | 仅有 `演示视频脚本.md`（脚本），**全项目无任何视频文件** |

> **一句话**：**"东西都做完了，但答辩要交的两样还差——一段演示视频，和三类缺陷的数据。**（后者的数据其实已经下到手了，只是还没并进去。）

---

## 二、赛题「必须交付」四项逐项核对

### ① 可运行的检测系统 —— ✅

| 载体 | 实测 |
|---|---|
| `06_deploy/app.py` | 47,964 B / **1,022 行**；Gradio 6+，`theme/css/head` 正确传给 `launch()`；含斜拍正射校正开关、JGJ 125 构件部位勾选组 |
| `02_code/pipeline.py` | 16,068 B，端到端推理主入口（单图 / 批量 / `--rectify` / `--jgj125`） |
| `08_photo_collector/site/` | `index.html` 39,102 B + `app.js` 13,340 B + `cloud.js` 4,643 B + `config.js` 990 B；**已云端发布**（https://facade-photo-collect.app.workbuddy.host/） |

运行说明齐备：`06_deploy/README.md`（2,727 B）、`07_report/README.md` "快速复现"节、技术报告 §10.4。

### ② 项目代码与部署文档 —— ✅

- `02_code/` 直下 **23 个 `.py`**：数据管线 `step0~step6`、训练 `train.py`、评测 `evaluate.py`、三组实验 `exp_*.py` + `run_experiments.py`、业务核心 `common/gsd/measure/grade/rectify/pipeline`、性能 `bench_pipeline.py`、校验 `_verify_rectify.py`、导出 `_extract_grading_rules.py`。
- 部署文档：`06_deploy/README.md`（依赖 / 运行 / 界面设计 / 已知环境限制）+ 技术报告 §10.2 代码清单（24 项逐一说明）+ §10.3 环境与已知坑。

### ③ 技术报告 —— ✅（结构远超赛题最低要求）

`07_report/技术报告.md` = 126,329 B，章节：

```
1 选题背景与问题定义      5 检测模型训练与选型    9 能力边界与诚实声明
2 核心创新：可判读性与选择性预测  6 几何量化与规范分级   10 工程复现说明
3 系统架构              7 实验验证            附录A 回填清单 / 附录B 四道答辩必答题
4 数据集建设            8 部署
```

配套：`答辩材料.md` 23,406 B（四道必答题 + 五步演示流程 + 9 个追问预案 + 逐字稿）、`演示视频脚本.md` 13,456 B。

### ④ 演示视频或实机展示 —— ❌ **缺口**

实测扫描全项目 `.mp4 / .mov / .avi / .mkv / .gif / .webm`：**结果为空**。

- 仅有 `07_report/演示视频脚本.md`（5 分钟分镜 + 3 分钟精简版 + 录制注意事项）。
- 技术报告附录 A 自己也记着：`§8.4 演示视频文件 —— 脚本已有，视频未录（需本人录屏）`。
- 赛题写的是"演示视频**或**实机展示"——两者都需**留痕**（视频文件 / 现场照片）。目前都没有。
- **这是必须由您本人操作的一项**（需真实录屏/拍照），我无法代做。

---

## 三、加分项（30 分）可得性对照

| 加分项 | 分值 | 现状 | 备注 |
|---|---:|---|---|
| 数据增强创新 | +3 | ⚠️ 有设计（`mosaic=0.5`、`flipud=0.0`、`close_mosaic=10`）但**无专门论证章节** | §5.1 只作为超参表说明，未提炼为"增强创新" |
| 模型结构改进（注意力/损失） | +5 | ❌ **未做** | 用的是原版 `yolov8s` / `yolo11s`，未改结构或损失 |
| 部署优化（转换/量化/加速） | +5 | ⚠️ 弱 | §8 有完整 INT8 量化记录，但**结论是"不可用"**（三档端到端检出均为 0）；FP32 ONNX 已导出 |
| 实时性优化 | +2 | ✅ | §7.5 `bench_pipeline.py` 分层耗时实测（load/predict/measure/draw/save） |
| 做成 Web / 移动应用 | +3 | ✅ | Gradio Web 演示 + 云端照片收集站 |
| 实机演示或用户测试反馈 | +3 | ❌ | 视频未录；收集站**投稿数 = 0** |
| 鲁棒性测试 | +4 | ✅ | §7.3 四组三档共 12 配置（模糊/噪声/低照度/压缩） |
| 跨学科融合 | +3 | ✅ | 结构工程（GB 50010 / JGJ 125）+ 摄影测量（GSD 可判读性） |
| 开源贡献价值 | +2 | ❌ | 未见开源仓库 / 权重发布载体 |

> **提示**：可拿的分里，**"实机演示/用户反馈"（+3）**与**"模型结构改进"（+5）**是两个明显空档。前者靠录视频 + 拉投稿就能补；后者若要补则需 GPU 重训，成本较高。**"开源贡献"（+2）**成本最低，建个仓库放代码即可。

---

## 四、★ 缺口清单

### 🔴 P0 —— 阻断交付 / 答辩必被追问

#### A1. 演示视频缺失（赛题"必须交付"之一）
见 §二④。**需您本人录屏/拍照**，脚本已备好（`演示视频脚本.md`）。

#### A2. 三类缺陷零数据 —— 而可用数据**已经下到手、却未并入**

当前训练集 `01_data/dataset/` 实测（`logs/_AUDIT_CLASSES.txt`）：

```
class              train图  val图  test图  test框
crack                143     41     22      58
spalling             111     29     19      30
efflorescence        269     73     36     118
exposed_rebar          0      0      0       0   ❌
rust                   0      0      0       0   ❌
delamination           0      0      0       0   ❌
moss                  80     20     10      53
```

**7 类体系里"设计 7 类、实际 4 类有数据"** —— 这是报告已诚实声明的（§4.4、§9.1、README 诚实声明），但答辩时评委一定会问"那三类为什么不补？"

**而实际上，补这三类的数据已经在 `01_data/raw/_public_datasets/` 里下好了**（详见 §六）：
- `rust` / `delamination` ← `urban_infra`（1.1 GB，13 类，含 `rust` 5,914 图 / `delamination` 746 图 / `void` 516 图）
- `exposed_rebar` ← `hrcds_yolo_exposed_rebar`（**已解压、已是 YOLO 格式 images+labels**，78.7 MB）

**但它们一个都还没被解压 / 判读 / 并入 / 重训。** 也就是说：**"数据缺口"已经从"找不到数据"变成了"有数据没用"。** 这两种状态在答辩上的性质完全不同。

> ⚠️ 按当前约定：**并入会改变全部已报指标**（需 GPU 重训）。这一步**需要您拍板**，我不会擅自执行。

#### A3. 四份对外报告均未反映新到手的 4 个数据源

`07_report/README.md` 与 `技术报告.md §4.1` 的**数据来源表只列了 2 个源**（`greybrick-yolo` + `jiange1236/StructuralCrackDataset`）；§4.4 的调研结论仍写：

> "RF100 中**没有任何建筑立面缺陷数据集** …… 这不是检索能力问题，而是该细分领域数据**真实稀缺**"

**这句话现在只对了一半** —— ModelScope/RF100 上确实没有，但通过 Kaggle + Mendeley 已找到 **MBDD2025（14,471 图建筑外墙）/ urban_infra / BFDD / HRCDS** 等 4 个源。报告若维持原表述，等于**隐瞒了自己已取得的检索成果**；若答辩被追问"你到底找了多少数据"，会答不上来。

**建议**：在 §4.4 增加一小节"补充数据源检索的后续进展（已获取、待并入）"，如实列出 4 个源的规格、许可与待办。

#### A4. 答辩 PPT 未开始
您先前明确"先不做"。若答辩需要，这是最后的空缺项。

---

### 🟠 P1 —— 一致性 / 可信度

| 编号 | 位置 | 问题 |
|---|---|---|
| **B1** | `技术报告.md:1812`（§10.1 目录结构） | 仍写 `images/{train,val,test}  455 / 129 / 65` —— **是 V1 数字**，与全篇 V2 口径 **473 / 134 / 67** 冲突 |
| **B2** | `技术报告.md:976`（§7.1.1） | 举例写 `spalling` 的 AP50 = **0.7457**，而 §7.1 定稿表写 **0.7319** —— 同一报告内两处不等 |
| **B3** | `logs/_MBDD2025_VERDICT.md §六` | **类别清单写错**：文中列"我们现有 7 类 = crack / spalling / **corrosion** / efflorescence / exposed_rebar / rust / delamination" —— **漏了 `moss`、多出一个并不存在的 `corrosion`**；并据此给出 `corrosion → corrosion 语义完全一致` 的映射建议（**我们的体系里没有 corrosion，第 7 类是 moss**） |
| **B4** | `自检报告_20260920.md` | 仍是 **V1 快照**（§2.2 写 455/129/65；§2.4 是旧指标；§四 #7 与 §7.3 末行仍写"V2 重训进行中"）—— 而 V2 已定稿。报告自己声明"问题清单保持原样作快照"，但**§四/§7.3 的"进行中"已过期**，会误导读者 |
| **B5** | `02_code/` | 目录污染仍在：`runs/detect/val*` **41 个空目录** + `_verify_out/`（37 文件 36.9 MB）+ `__pycache__/_weights`。自检称已归档，实测**未清干净** |
| **B6** | `logs/` | `_trash_here/`（45 files / **4.6 GB**）+ `_famerL_tmp/`（**1.7 GB**）= **6.3 GB**，占 `logs/` 总容量的绝大部分；另有约 **50 个 `_backup_*` 报告快照子目录**。交付前需决定是否清理 |
| **B7** | `06_deploy/app.py` | 自检 P2-2 遗留：**行号对不上且未定案**。实测 `442-443` 现为 `_default()` 的 `raise TypeError`、`178-179` 现为 `cv2.cvtColor` / `h, w = img.shape[:2]`（**并非死代码**）。自检已声明"不猜测、不擅改"，**至今未结** |

---

### 🟡 P2 —— 清理 / 冗余

| 编号 | 位置 | 说明 |
|---|---|---|
| C1 | `04_results/eval/` | `v8s640_final_test_metrics.json`（2,274 B）与 `v8s640_test_metrics.json`（2,251 B）**并存两套**，需确认哪套是定稿口径 |
| C2 | `01_data/raw/_public_datasets/` | **约 4.4 GB 压缩包未解压**（mbdd2025 2.4 GB / urban_infra 1.1 GB / bfdd 0.5 GB / hrcds 0.2 GB / rc1841 0.14 GB / rc2119 0.15 GB） |
| C3 | 全树 | **96 个空目录**（`02_code/runs/detect/val-*` 41 个 + `_archive_.../runs_ultralytics_val_plots/` 约 40 个 + `01_data/raw/_archived_gitempty_*` / `greybrick/src/{train,valid}/{images,labels}` 等） |
| C4 | 项目根 | `_grun.txt`（279 B）残留（其占用的 7861 端口已释放） |
| C5 | `01_data/` | `unified/*`、`dedup/*` 多处**空壳目录**（0 files）—— 中间产物已归档但目录留壳 |
| C6 | `_archive_20260920/` | 3.9 GB / 7,736 files；交付包里是否保留需决策 |

---

## 五、报告内部矛盾点逐条定位（便于逐项修订）

| # | 文件:行 | 现文 | 应为 |
|---|---|---|---|
| 1 | `技术报告.md:1812` | `455 / 129 / 65` | `473 / 134 / 67` |
| 2 | `技术报告.md:976` | `spalling` AP50 = `0.7457` | `0.7319`（与 §7.1 定稿表一致） |
| 3 | `_MBDD2025_VERDICT.md` §六 | 7 类含 `corrosion`、漏 `moss` | `crack / spalling / efflorescence / exposed_rebar / rust / delamination / moss` |
| 4 | `自检报告_20260920.md` §四 #7、§7.3 | "V2 重训进行中" | "V2 已定稿（见 07_report/README.md 核心数字速查）" |
| 5 | `07_report/README.md` 数据来源表 | 仅 2 个源 | 增补 4 个新源（或明确标注"已获取待并入"） |
| 6 | `技术报告.md §4.4` | "该细分领域数据真实稀缺" | 补一句"MBDD2025 等 4 源已获取，待并入" |

> 第 1、2 条是**纯数字笔误**，5 分钟可修；第 3、5、6 条涉及**事实陈述的完整性**，建议按 §四 A3 整段增补。

---

## 六、四个新数据源的实际状态（供"是否并入"决策）

`01_data/raw/_public_datasets/` 实测 = **8,712 files / 5,518.5 MB（5.4 GB）**

| 数据集 | 形态 | 大小 | 内容与价值 |
|---|---|---:|---|
| `mbdd2025` | zip **未解压** | 2,414.2 MB | 14,471 图建筑外墙（无人机，合肥）；5 类 `crack` 6,253 图 / `abscission` 5,166 / `corrosion` 2,102 / `leakage` 2,464 / `bulge` 1,337；**YOLO txt 已就绪**；CC BY 4.0 |
| `urban_infra` | zip **未解压** | 1,108.7 MB | 13 类；含 ★`rust` 5,914 图、★`delamination` 746 图、`void` 516 图（仅 train）；YOLO `.txt` 零转换成本 |
| `bfdd` | tar.gz **未解压** | 527.7 MB | 838 对 RGB+IR+Label（5 类分割掩膜，640×512）；**红外可观测亚表面脱层** —— "空鼓需红外"论点的学术依据 |
| `hrcds` | zip **未解压** | 195.4 MB | 露筋源（HRCDS 原包） |
| **`hrcds_yolo_exposed_rebar`** | **已解压 images+labels** | 78.7 MB | ★ **露筋，已是 YOLO 格式，可直接接入** |
| `construction_defect_yolo` | 已解压 | 851.0 MB | 待判读（7,451 files） |
| `roi1555` | 已解压 | 46.9 MB | 待判读（323 files，子目录 `1260`） |
| `rc1841` | zip 未解压 | 143.1 MB | 1,841 图，含 major spalling / delamination |
| `rc2119` | zip 未解压 | 152.8 MB | 待判读 |

**结论**：`exposed_rebar` 的补数据**已经是现成的 YOLO 格式、无需转换**；`rust` / `delamination` 的数据在 `urban_infra` 里（也不需转换）。**唯一缺的是"解压 → 判读 → 按 7:2:1 分层并入 → GPU 重训 → 重跑全部指标"这条流水线，以及您的拍板。**

> 相关判读报告已完成：`logs/_URBAN_VERDICT.md`、`logs/_MBDD2025_VERDICT.md`、`logs/_HRCDS_VERDICT.md`、`logs/_FAMERL_VERDICT.md`。
> ⚠️ **`BFDD` 的独立判读报告尚未落盘**（素材在 `logs/_preview_bfdd/`、`logs/_preview_bfdd_classes/`、`logs/_bfdd_deep.txt`）。

---

## 七、未完成事项（承前 + 本次新增）

| # | 事项 | 阻塞原因 | 能否由我推进 |
|---|---|---|---|
| 1 | **演示视频** | 需本人录屏 | ❌ 需您 |
| 2 | **四源数据并入 + GPU 重训** | 需您拍板（会改变全部已报指标） | ⏸ 待拍板 |
| 3 | 四份报告增补新数据源陈述 | 依赖 #2 的决策 | ⏸ 待拍板 |
| 4 | 报告两处数字笔误修订（§五 #1/#2） | 无 | ✅ 可立即做 |
| 5 | `_MBDD2025_VERDICT.md` 类别表纠正（§五 #3） | 无 | ✅ 可立即做 |
| 6 | `自检报告` V2 状态回填（§五 #4） | 无 | ✅ 可立即做 |
| 7 | 答辩 PPT | 您先前"先不做" | ⏸ 待拍板 |
| 8 | 自拍照片补充 | 等网友投稿（当前 0 条） | ⏸ 待投稿 |
| 9 | 清理：`logs/_trash_here` 4.6 GB + `_famerL_tmp` 1.7 GB + 96 空目录 + `02_code/runs` | 无 | ✅ 可立即做（**需您确认，因涉及删除**） |
| 10 | `BFDD` 判读报告落盘 | 无 | ✅ 可立即做 |
| 11 | `app.py` P2-2 行号核对定案 | 无 | ✅ 可立即做 |
| 12 | 最终工程包 → `D:\pythonstudy 备份\种子杯项目报告` | 按约定，仅全部完成后拷贝 | ⏸ 待全部完成 |

---

## 八、下一步建议（按"投入产出比"排序）

1. **【必修 · 5 分钟】** 修两处数字笔误（`技术报告.md:1812` → 473/134/67；`:976` → 0.7319）与 `_MBDD2025_VERDICT.md` 类别表。
2. **【必修 · 30 分钟】** 在 `技术报告.md §4.4` + `07_report/README.md` 增补"补充数据源已获取（4 源）、待并入"一段 —— 这是**答辩防追问**的关键，也是把"找不到数据"改写成"数据已到位"的正名。
3. **【高价值 · 需拍板】** 决定是否把 `hrcds_yolo_exposed_rebar`（现成 YOLO）+ `urban_infra`（rust/delamination）并入并 GPU 重训。**这会实质消除 3 类零数据**，是本项目唯一能提升"检测效果 15 分"与"完成质量"的途径。
4. **【必做 · 需您】** 录演示视频（脚本已备）。
5. **【可选 · 低成本】** 建开源仓库放代码/权重（+2 分）。
6. **【收尾】** 清理 6.3 GB 垃圾 + 96 空目录，再执行最终工程包拷贝。

---

## 九、留痕（本次审查新建的只读产物）

| 文件 | 内容 |
|---|---|
| `logs/_audit_inventory.py` → `logs/_AUDIT_INVENTORY.txt` | 项目全貌（顶层/二层/权重/文档/数据集） |
| `logs/_audit_classes.py` → `logs/_AUDIT_CLASSES.txt` | 当前数据集 7 类分布（确认 3 类为零） |
| `logs/_audit_more.py` → `logs/_AUDIT_MORE.txt` | 02_code / 05 / 06 / 07 / 08 完整文件清单 |
| `logs/_audit_more2.py` → `logs/_AUDIT_MORE2.txt` | 视频扫描 / eval 清单 / 空目录统计 / app.py 行号核对 |
| `logs/_audit_datasrc.py` → `logs/_AUDIT_DATASRC.txt` | 新数据源路径与形态确认 |
| **本报告** `审查报告_交付物完整性与缺口.md` | 结论汇总 |

> 以上脚本**只读**，不对任何文件执行写入 / 改名 / 删除。
