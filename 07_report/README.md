# 外墙缺陷智能筛查系统 —— 交付物索引

> **赛事**：第六届萌新种子杯 · 视觉赛道 · 创新题
> **作者**：北京建筑大学
> **项目根目录**：`D:\pythonstudy 备份\创新题\外墙缺陷筛查`

---

## 一句话定位

> 我们做的不是一个更准的裂缝检测器，而是第一个**知道自己什么时候不可靠**
> 的外墙筛查系统 —— 因为在房屋安全这件事上，把「判不了」说成「没问题」
> 比漏检更危险。

---

## 报告文档与提交物（`07_report/`）

> **创新题提交物共三件**：① 答辩 PPT ② 项目介绍书（500 字）③ 实机运行视频。
> 三者在本目录均以「**提交物**」标注；其余是配套的报告文档与留档。

| 文件 | 内容 | 适用场合 |
|------|------|---------|
| [`外墙缺陷智能筛查_答辩PPT.pptx`](外墙缺陷智能筛查_答辩PPT.pptx) | **答辩演示 PPT**（20 页） | 提交物 ① |
| [`项目介绍书.md`](项目介绍书.md) · [`项目介绍书.docx`](项目介绍书.docx) | **项目介绍书（493 字）**，md 与 docx 同源 | 提交物 ② |
| [`外墙缺陷智能筛查_实机运行视频.mp4`](外墙缺陷智能筛查_实机运行视频.mp4) | **实机运行视频**（真实系统跑起来的录屏） | 提交物 ③ |
| [`技术报告.md`](技术报告.md) | 完整技术方案：背景、创新点、架构、实验、部署、边界声明 | 技审材料（70 分） |
| [`答辩材料.md`](答辩材料.md) | 四道必答题标准答法 + 演示流程 + 追问预案 + 逐字稿 | 选题答辩（30 分） |
| [`演示视频脚本.md`](演示视频脚本.md) | 5 分钟分镜脚本 + 3 分钟精简版 + 录制注意事项 | 备查 |
| [`_not_submitted_演示视频/`](_not_submitted_演示视频/) | 此前的**合成**演示视频（PIL 关键帧 + TTS 旁白），**不提交**，仅留档 | 留档 |
| [`先例与创造性核查_20260923.md`](先例与创造性核查_20260923.md) | **答辩必备**：本方向的既有先例（含出处）与我们的真实增量划界 | 答辩准备 |
| [`创新方向评估_20260923.md`](创新方向评估_20260923.md) | **还能往哪创新**：4 个可做方向（含赛道契合、成本、证据要求）+ 2 个不建议方向 | 答辩准备 |

### 实机运行视频（提交物 ③）

| 项目 | 值 |
|------|-----|
| 时长 | **2:18.43** |
| 大小 | 23.07 MB |
| 规格 | **1920×1080 / 30 fps / H.264 (High) / yuv420p** |
| 音轨 | **无**（纯录屏，按用户要求不加字幕、不加旁白） |
| 录制方式 | Playwright 驱动真实 Chromium 打开本项目的 Gradio 应用（`06_deploy/app.py`），自动上传照片、设置参数、点击「开始分析」并等待真实推理结果 |
| 生成脚本 | `logs/_record_demo.py`（Playwright 录制三幕 .webm）→ `logs/_encode_final.py` 用 **imageio-ffmpeg 自带 ffmpeg 7.1**（含 libx264）合成：`-c:v libx264 -preset slow -crf 20 -r 30 -an`，段间 0.45s 淡入淡出 |
| 与界面的一致性 | **重录于 2026-09-24 23:22** —— `app.py` 于 09-23 17:48 新增了**可见 UI**（结论区「第二重判据：成像质量」表 + 「判据推导（可复算）」表），而旧片摄于 09-23 14:13、早于该改动，故必须重录。现成片与当前 `app.py` 界面一致（旧片备份于 `logs/_video_stale_backup/`，交付包内旧片另存于 `_backup_before_sync_20260924_232342/`） |

**三幕对照** —— 同一套系统、同一权重，只改输入，把「判不了 / 安全 / 不安全」都演到：

| 幕 | 图片 | 参数 | 系统输出 | 风险等级 |
|----|------|------|---------|---------|
| 1 | 泛碱墙 | 相机参数估算 · 20 m（**全默认**） | 「**不可判读 —— 系统拒绝给定量结论**」，13 条定量结论全部作废 | **[U] 无法判定** |
| 2 | 同上 | 切「**标定物**」420 px / 210 mm | 「**部分可判读 —— 按类别区别对待**」：块状类给出 mm² 与分级，裂缝类仍拒答 | **[A] 安全** |
| 3 | **空鼓墙** | 同幕 2（标定物 420 px） | 「**部分可判读**」：空鼓分层面积 **1.00 cm²** 判为「关注」 | **`[B] 个别危险点`**，**是否需要专业机构进场：是** |

> **三幕合起来才是完整的演示**：
> 幕 1 证明系统**敢说「判不了」**（不把「判不了」报成「没问题」）；
> 幕 2 证明它**不因拒答而丢掉本可测准的结果**（避免过度拒答）；
> 幕 3 证明它**真的会报出「不安全」并要求专业机构进场**，不是一个只会说「安全」的摆设。

> ⚠️ **幕 3 为何用「空鼓」而非「漏筋」**：实测 `app.py:311-316` 的 `_is_line()` 把
> `exposed_rebar` 与 `crack` 归为同一类、**用 0.3 mm 裂缝判据卡可判读性** ⇒
> 0.3 mm 在图上仅 0.6 px，**漏筋永远达不到 8 px 门槛、永远被拒答**（实测 14 处漏筋检出
> 全部作废、等级输出 U）。而空鼓 `delamination` 按块状类（10 mm 判据）判定，
> 且 `grade_blob` 规定其**至少 attention** ⇒ 能稳定输出 B 级。**该 gating 是否合理见待决项。**

| 幕 | 图片文件（`01_data/dataset/images/test/`） |
|----|------|
| 1 / 2 | `greybrick-yolo__2315.jpg`（448×448，泛碱 13 框） |
| 3 | `urban_yolo_rust_delam__urban_train_00924_jpg.rf.ee1afa3581042dd5d5d6f9309ad032f5.jpg`（640×640，空鼓 4 框） |

> ⚠️ **画面右侧留白是应用自身的设计**：`06_deploy/app.py` 的 CSS 将
> `.gradio-container` 限宽 `max-width:1280px`（`margin:0`，左对齐），
> 1920 视口下右侧必然留白 640 px。录制**如实呈现该外观**，未做任何界面改动。

#### 想自己跑起来看（复核/答辩现场）

这个「实机演示」不是独立软件，而是本项目的一个**本地网页应用**：

| 项 | 位置 |
|----|------|
| 程序本体 | `06_deploy/app.py` |
| **双击启动** | **`06_deploy/run_app.bat`** ← 自动起服务并打开浏览器 |
| 用法说明 | `06_deploy/启动说明.txt` |
| 模型权重 | `03_weights/v11s640_best.pt` |
| 依赖源码 | `02_code/`（`gsd.py` / `measure.py` / `grade.py` / `pipeline.py`） |
| 访问地址 | `http://127.0.0.1:7860`（仅本机，不对外网开放） |

> 命令行等价做法：在 `06_deploy/` 下执行 `D:\下载\python.exe app.py`。
> 首次点「开始分析」需加载模型（约 5-15 秒），之后每次约 1 秒。
> **不要加 `--share`**：本机网络走外网隧道会 502。

### 答辩 PPT（提交物 ①）

**20 页**，主色 `#2563EB`／辅 `#0891B2`／强调 `#7C3AED`，微软雅黑，编号式项目符号。
结构：封面 → 目录（6 章）→ 逐章内容（含 1 张 GSD 柱状图、2 张数据表、1 张端到端流程图）→ 结尾。
内容严格对应 `答辩材料.md` 的四道必答题与 `技术报告.md` 的边界声明章节。

### 项目介绍书（提交物 ②）

**正文 493 个中文汉字**，另加标题 5 字 ⇒ 全文 498 字（按中文汉字计；标点不计）。
四段结构：① 题眼 ② 问题与物理约束 ③ 做了什么 ④ 结果与边界。
数字与口径取自 `技术报告.md` §1 与 V3 实测结果，**未改写任何数字**。
`项目介绍书.docx` 由 `项目介绍书.md` 渲染（正文一致，另加页脚标注项目与单位）。


---

## 核心数字速查

> ✅ **精度数字已按 V3 回填（2026-09-21）**：数据集 **2848 图 / 18262 框 / 7 类**
（技术报告 §4.1.1），划分 **1995 / 568 / 285**；四档模型已在 V3 上重训并重评估。
> 下表精度类数字**均为 V3 实测值**（测试集 285 图 / 1646 实例 / 7 类有标注）；
> **数据规模类数字已按 V3 更新**。主力模型为 `v11s640`（`03_weights/v11s640_best.pt`）。

| 指标 | 值 | 说明 |
|------|-----|------|
| 判读 0.3mm 裂缝所需 GSD | **0.1 mm/px** | 物理硬约束，0.3mm ÷ 3px |
| 主摄(24mm)所需拍摄距离 | **27 cm** | 地面拍高层做不到 |
| 像素门槛 | **3 px / 8 px** | 可判读 / 可精测 |
| 数据集规模 | **1995 / 568 / 285** ✅ | train / val / test（**V3**；V2 为 473 / 134 / 67） |
| 类别体系 | **7 类设计，7 类有数据** ✅ | V3 起三类已补齐（`exposed_rebar` 1389 / `rust` 13685 / `delamination` 694 框） |
| **主力** | **`v11s640`** | ⚠️ **按 mAP50 单指标，`v8s640` 反而高 0.0126** —— 主力地位由**三项非精度优势**支撑（见下方说明） |
| **`v11s640` mAP50** | **0.7230** | V3 独立测试集 285 图 / 1646 实例（7 类有标注）；V2 为 67 图 / 259 实例（4 类），两版不可比 |
| **`v11s640` 逐类 AP50** | 见下表 | crack 0.728 / spalling 0.773 / efflorescence 0.841 / exposed_rebar **0.457** / rust 0.739 / delamination 0.700 / moss 0.822 |
| **`v11s640` mAP50-95** | **0.44105** | 四档**最高**（比 v8s640 的 0.43244 高 0.0086） |
| ⚠️ **最弱两类（必须一并说）** | `exposed_rebar` / `crack` | AP50-95 仅 **0.264** / **0.335**（线状细长目标），与 §9.1 的测量窗口边界同源 |
| **`v11s640` 推理延迟** | **9.29 ms/图**（p50） | RTX 5070 Ti Laptop (CUDA)，ultralytics 口径的纯 inference（前后处理另计）。**引用时必须写口径** |
| `v8s640` 测试集 mAP50 | 0.7356 | 版本对照。**mAP50 上反而高于主力 0.0126**（未超 ≈0.03 判读阈值，判不出高下）；但 mAP50-95 0.43244、体积 21.51 MB，均落后 |
| `v8s1024` 测试集 mAP50 | 0.7143 | 小目标实验（训练 1024 / 评估 640，**该行被低估**） |
| `v8n640` 测试集 mAP50 | 0.7172 | 轻量档（5.99 MB）；与 v8s 差 0.0184 < 0.03，判不出高下 |
| 域偏移崩溃点 | **4.0 mm/px** | 与理论门槛吻合（§7.2） |
| 鲁棒性最低档 | **mAP50 0.159** | 低照度 level 1.0，几何合格≠图像可用（§7.3） |
| INT8 可用性 | **不可用** | 三档 INT8 端到端可用性已按 V3 权重复核；本项目**不走赛道二**，详见 §8.2.1 |

> ⚠️ **为何主力仍定 `v11s640`（别被上面第一行误导）**：V3 扩容后，四档 mAP50
> 落在 **0.7143~0.7356**，极差仅 **0.0213 < 0.03 判读阈值** —— **四个模型在 mAP50 上判不出高下**。
> 定 `v11s640` 的依据是**三项非精度优势**：① mAP50-95 **最高**（0.44105，比 v8s640 高 0.0086）；
> ② 体积最小（**18.33 MB** vs v8s640 21.51 MB）；③ 假阳更少（**V2 口径** 109 vs 119，须注明口径）。
> 完整对照与口径纪律见 `技术报告.md` §5.3.1 / §7.4A。

<!-- v3bf:rdt -->

> ⚠️ **本项目不走赛道二（轻量化），也不把量化作为交付目标。**
> 技术报告 §8 保留的是**早期工程尝试记录**：三档配置（`v8n` / `v8s` / 主力 `v11s640`）
> INT8 后**端到端检出全部为 0**。其中主力 `v11s640` 的 INT8 体积 9.54 MB
> **恰好压进 10 MB 门槛，但同样检不出目标** —— 2026-09-20 的受控抢救试验
> （校准集 32/16 × MinMax/Entropy 四档组合）结果**四次完全一致**，
> 说明问题在 **PTQ 方法层面，非调参可解**。
> **「体积达标」是可以骗人的指标** —— 如实报告，不包装成达标。

---

## 项目结构与关键文件

```
外墙缺陷筛查/
├── 01_data/                    数据（原始 / 去重 / 统一 / 最终数据集）
│   ├── dataset/                训练集：images + labels + wall_defects.yaml
│   └── audit/                  数据阶段审计报告
├── 02_code/                    全部源码（**21 个正式模块**，另有 **7 个内部校验/探针脚本**）
│   ├── gsd.py                  ★ 核心：GSD 标定与可判读性判定
│   ├── measure.py              ★ 核心：OpenCV 几何量化
│   ├── grade.py                ★ 核心：国标分级规则
│   ├── rectify.py              ★ 核心：斜拍正射校正 + 多段拼接（含周期混叠拒答）
│   ├── pipeline.py             端到端推理主入口
│   ├── train.py                训练（4 组配方 + 泄漏复检 + 断点续训）
│   ├── evaluate.py             独立测试集评估
│   ├── step0~step6*.py         数据流水线 → 划分 → 量化
│   └── exp_*.py                三组实验（域偏移 / 鲁棒性 / 消融）
├── 03_weights/                 预训练权重 + 训练产出归档
├── 04_results/                 所有实验产出
│   ├── train/                  训练输出（含 results.csv 学习曲线）
│   ├── eval/                   测试集评估 / 鲁棒性 / 域偏移
│   ├── ablation/               消融实验
│   ├── quant/                  FP32+INT8 ONNX + 量化报告
│   └── vis/                    可视化标注图
├── 05_quantify_grade/          ★ 国标分级专项（判据清单，机器可读 + 可追溯）
├── 06_deploy/app.py            Web 演示应用（Gradio）
├── 07_report/                  本目录：报告文档
├── 08_photo_collector/site/    ★ 网友照片收集站（已云端发布）
│   ├── index.html              单页应用：登录/注册 + 上传
│   ├── app.js                  页面逻辑
│   ├── cloud.js                云服务接入层（Auth/Database/Storage）
│   └── config.js               publicConfig（可公开，无密钥）
└── logs/                       运行日志
```

### 照片收集站（`08_photo_collector/`）

为获取「自拍补充」样本而建的线上收集页，可转发给网友帮忙拍摄。

- **线上地址**：https://facade-photo-collect.app.workbuddy.host/
- **收集字段**：照片 + **拍摄距离**（关键 —— 决定 GSD，是量化分析的输入）
- **存储**：云端数据库记录元数据，云端对象存储保存图片
- **设计原则**：**页面不做任何缺陷判断**，只负责收数据，
  避免给网友错误的安全印象（与 §9 的边界声明一致）
- **安全**：公开可读（用于展示已收集数量）、**仅登录用户可写**；
  匿名写入已被 RLS 拒绝（实测 `permission denied`）

---

## 快速复现

```powershell
$PY = "D:\下载\python.exe"
cd "D:\pythonstudy 备份\创新题\外墙缺陷筛查\02_code"

# 训练（若已有权重可跳过）
& $PY train.py --runs=v8s640,v11s640

# 测试集评估（报告中引用的数字只能来自这里）
& $PY evaluate.py --all

# 实验（主力权重：03_weights/v11s640_best.pt）
& $PY exp_domain_shift.py --weights=..\03_weights\v11s640_best.pt
& $PY exp_robustness.py   --weights=..\03_weights\v11s640_best.pt
& $PY exp_ablation.py     --weights=..\03_weights\v11s640_best.pt

# INT8 量化（早期尝试记录，本项目不走赛道二；默认权重已是 v11s640）
& $PY step6_quantize.py

# 单图推理
& $PY pipeline.py --image=wall.jpg --model=..\03_weights\v11s640_best.pt --calib-object-px=420

# Web 演示
& $PY ..\06_deploy\app.py
```

---

## 三个最值得记住的技术点

### 1. 可判读性判定（创新核心）

`可判读性 = 目标实际尺寸 ÷ GSD`，门槛 3 像素。
系统对每张图**分别**判「裂缝定级」（0.3mm）和「块状筛查」（10mm），
因为二者差两个数量级。判据是**可解释的几何量**，评委可以拿计算器复算。

### 2. 拒答必须「传染」到输出层

⚠️ 这是实践中发现的非平凡缺陷：
若顶部提示「不可判读」，明细表却照打印「裂缝宽 230.19mm（严重）」，
使用者只会记住「严重」。**自相矛盾的输出比不输出更危险。**
因此一旦判定不可判读，该类所有条目的定量结论全部作废 ——
**连 `reason` 文本都重写**，确保荒唐数字不会从任何字段漏出。

### 3. 避免「过度拒答」

GSD=0.5mm/px 时，裂缝（0.3mm）判不了，但剥落（10mm 占 20px）完全可测。
若用总判定盖全，会丢掉本来算准的结果。
故系统**按类别分别判定、分别输出**。

---

## 诚实声明（必须与结论同时呈现）

1. **本系统是筛查工具，不是法定鉴定工具。**
   达到 B/C 级应转由具备资质的专业机构进场鉴定。
2. **类别体系设计 7 类 —— V3 起 7 类全部有数据支撑。**
   V2 时只有 4 类（crack / spalling / efflorescence / moss）拿得到数据：
   露筋仅 **9 框 / 2 图**、锈迹与空鼓为 **0**。
   V3 起并入 **HRCDS**（露筋 **462 图 / 1389 框**，Mendeley）与
   **Urban**（锈迹 **1196 图 / 13685 框**、空鼓 **518 图 / 694 框**，Kaggle）后，
   数据集由 **674 图**扩到 **2848 图 / 7 类 / 18262 框**（见 §4.4 / §4.1.1）。
   **但两条限制必须同时声明**：
   ① **类别极度不平衡** —— `rust` **13685** 框 vs `spalling` **313** 框 = **43 : 1**，
   且**未做重采样**（保持数据原貌）⇒ **整体 mAP 会被 `rust` 抬高**，
   引用总体指标时必须同时给逐类数字；
   ② **空鼓**即便拿到了标注，可见光下的判读仍只是**间接迹象**，不等于对内部
   脱开的可靠判读 —— 见下方第 3 条。
   （附注：露筋补充源里另有 **79 个 `crack` 框 / 25 图，V2 起已并入主数据集**；
   该源的 `exposed_rebar` 9 框与 `spalling` 2 框继续隔离，见 §4.1.4。）
3. **空鼓在可见光下本质上无法检测** —— 脱开发生在内部界面，
   表面可能完全正常；专业手段是敲击法或红外热成像。这是技术边界，
   不是数据问题。
4. **能力边界由几何决定**：判读 0.3mm 裂缝需 GSD ≤ 0.1mm/px，
   地面拍摄无法达到。系统会主动拒绝此类结论。

---

## 数据来源与合规

| 数据源 | 用途 | 许可 |
|--------|------|------|
| `greybrick-yolo` | 主力数据（裂缝/剥落/泛碱/苔藓） | 见源站 |
| `jiange1236/StructuralCrackDataset` | 露筋补充源（**27 图 / 90 框**；其中 79 个 `crack` 框已并入主集，`exposed_rebar` 9 框隔离） | Apache-2.0 |

通过 ModelScope 国内镜像获取。补充数据源的三轮穷尽式调研结论见
`D:\pythonstudy 备份\.workbuddy\reference\补充数据源调研结论.md`。

### 补充数据源（2026-09-21 获取 → 2026-09-21 已全部并入 V3 并重训）

除上表两个源之外，本项目又通过 **Kaggle 公开 API**、**Mendeley Data**、**hf-mirror 国内镜像**
获取到 4 个可用数据集。**这四个源已全部并入 V3 数据集并完成四档重训重评估**，
故本文档与技术报告中的 V3 数字**已包含它们**（见 `技术报告.md` §4.4.1）。

| 数据集 | 许可 | 规模 | 并入 V3 后的用途 |
|--------|------|------|------|
| `MBDD2025` | CC BY 4.0 | 14,471 图（UAV 航拍外墙，YOLO txt 现成） | 补 `crack` / `spalling` 等基础类；含 `corrosion` / `leakage` / `bulge`（**语义待逐类核对**） |
| `Urban Infrastructure Anomalies` | Apache 2.0 | 13 类（YOLOv8 txt） | ★ 补 `rust` **13685 框** / `delamination` **694 框** —— 原两个零数据类 |
| `HRCDS` | CC BY 4.0 | 1,200 图（LabelMe 多边形） | ★ 补 `exposed_rebar` **1389 框** / 462 图 —— 已转 YOLO |
| `BFDD` | CC BY 4.0 | **838 对** RGB + 红外（640×512）<br>（官方说明书写 788 对，与实测不符 —— 见 §4.4.1 注） | 含空鼓；红外可视亚表面脱层 —— §9.2 论点的学术依据（**未作检测训练数据**） |

> ⚠️ **「类名相同 ≠ 定义相同」**：任何源并入前必须逐类看图核对定义
> （例：MBDD2025 的 `bulge` 是外凸鼓包，**不等于**本项目的内部空鼓 `delamination`）。


